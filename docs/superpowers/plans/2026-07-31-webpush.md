# Web Push 推送通知

> 分支 `feat/notifications`，与邮件渠道同属通知管线第二阶段。设计见 `docs/superpowers/specs/2026-07-31-webpush-design.md`。

## Tasks

- [x] Task 1：后端依赖与配置 — `web-push` + `@types/web-push`；config 新增 `vapid.{publicKey, privateKey, subject}`；`.env.example` 补充 `VAPID_*`
- [x] Task 2：`models/PushSubscription.ts` — 按用户+endpoint 唯一索引；`routes/push.ts`：`GET /api/push/config`（公钥，未配置返回 null）、`POST/DELETE /api/push/subscription`（upsert 去重、每用户上限 20 淘汰最旧、endpoint/base64 校验）
- [x] Task 3：`services/webpush.ts` — `WebPushChannel`（id='webpush'）：TTL 1 天、载荷含 title/body/url/tag/type/projectId、404/410 清除失效订阅、其他失败仅记日志；未配置 VAPID 时静默跳过（不阻塞邮件去重）；注册进 `notificationChannels`
- [x] Task 4：`backend/tests/push.test.ts` 10 用例 — 路由（401/公钥/upsert 去重/校验/上限淘汰/删除幂等）+ 渠道（载荷与 VAPID details、只推本人、410 清理、未配置跳过）
- [x] Task 5：前端订阅 — `lib/push.ts`（`subscribePush`/`unsubscribePush`/`getVapidPublicKey`，权限 granted 静默订阅）+ `components/PushBanner.tsx`（未询问过时展示一次开启条，可暂不；已授权自动订阅）
- [x] Task 6：`scripts/patch-sw.mjs` — 构建后向 generateSW 产物追加 `push`/`notificationclick` 监听（点击通知跳转 payload.url）；`npm run build` 串联执行
- [x] Task 7：文档 — features/readme/progress + 本计划/设计文档
