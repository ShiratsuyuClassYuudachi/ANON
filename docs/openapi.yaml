openapi: 3.0.3
info:
  title: ANON API
  version: 0.1.0
  description: 漫展/线下活动执行组协作工具 ANON 的 HTTP API 机器可读规范。各端点的详细语义（权限口径、业务规则、字段含义）以 docs/api.md 为准；本规范与其冲突时以代码与 docs/api.md 为准。
servers:
  - url: 'http://localhost:4000'
    description: 本地开发
security:
  - bearerAuth: []
tags:
  - name: 系统
  - name: 认证
  - name: 个人资料
  - name: 超管后台
  - name: 项目
  - name: 里程碑
  - name: 邀请接受
  - name: 文件
  - name: 待办
  - name: 公告
  - name: 工作台仪表盘
  - name: 项目动态
  - name: 风险预警
  - name: 定时提醒
  - name: 推送订阅
  - name: 财务
  - name: 物料管理
  - name: 实物清单
  - name: 平台账号
  - name: 现场任务单
  - name: 舞台 Rundown
  - name: 舞台报名审核
  - name: 失物招领
  - name: 自定义工具
  - name: OpenAPI 密钥
paths:
  /api/health:
    get:
      tags: [系统]
      summary: 健康检查
      security: []
      responses:
        '200':
          description: 服务正常
          content:
            application/json:
              schema:
                type: object
                required: [ok]
                properties:
                  ok: { type: boolean }
  /api/auth/register:
    post:
      tags: [认证]
      summary: 注册
      description: 普通用户必须带有效邀请码；当数据库无任何用户且配置了 SUPER_ADMIN_EMAIL 且邮箱匹配时，可不带邀请码注册并自动成为超级管理员。password ≥ 8 位。
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [email, name, password]
              properties:
                inviteCode: { type: string }
                email: { type: string, format: email }
                name: { type: string }
                password: { type: string, description: 至少 8 位 }
      responses:
        '201':
          description: 注册成功
          content:
            application/json:
              schema:
                type: object
                required: [token, refreshToken, user]
                properties:
                  token: { type: string }
                  refreshToken: { type: string }
                  user: { $ref: '#/components/schemas/User' }
        '400':
          description: 参数错误（bad_request / invalid_invite 邀请码无效）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '409':
          description: 邮箱冲突（email_taken 已注册 / email_reserved 试用账号邮箱保留）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '429':
          description: 限流（rate_limited，每 IP 15 分钟最多 50 次）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
  /api/auth/login:
    post:
      tags: [认证]
      summary: 登录
      description: 试用模式：邮箱等于 TRIAL_EMAIL（默认 admin@test.com，置空禁用）且未被真实用户占用时，按密码哈希进入独立演示环境，响应带 trialExpiresAt。
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [email, password]
              properties:
                email: { type: string, format: email }
                password: { type: string }
      responses:
        '200':
          description: 登录成功
          content:
            application/json:
              schema:
                type: object
                required: [token, refreshToken, user]
                properties:
                  token: { type: string }
                  refreshToken: { type: string }
                  user: { $ref: '#/components/schemas/User' }
                  trialExpiresAt: { type: string, format: date-time, description: 试用会话销毁时间（仅试用登录返回） }
        '400':
          description: 参数错误（bad_request，如试用密码不足 8 位）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '401':
          description: 凭证错误（bad_credentials）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '429':
          description: 限流（rate_limited / trial_limit 活跃试用环境超限）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
  /api/auth/refresh:
    post:
      tags: [认证]
      summary: 刷新凭证
      description: 用 refresh token 换新凭证对。refresh token 本身是凭证，无需 Authorization 头。每次刷新轮换：旧 refresh token 立即作废；access token 有效期 15 分钟。
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [refreshToken]
              properties:
                refreshToken: { type: string }
      responses:
        '200':
          description: 新凭证对
          content:
            application/json:
              schema:
                type: object
                required: [token, refreshToken, user]
                properties:
                  token: { type: string }
                  refreshToken: { type: string }
                  user: { $ref: '#/components/schemas/User' }
        '400':
          description: 缺少 refreshToken（bad_request）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '401':
          description: refresh token 无效/已过期/已轮换/已吊销（invalid_refresh）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '429':
          description: 限流（rate_limited）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
  /api/auth/logout:
    post:
      tags: [认证]
      summary: 登出
      description: 吊销 refresh token（登出后不可再刷新）。无 token 时也返回成功。
      security: []
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                refreshToken: { type: string }
      responses:
        '200':
          description: 已登出
          content:
            application/json:
              schema:
                type: object
                required: [ok]
                properties:
                  ok: { type: boolean }
        '429':
          description: 限流（rate_limited）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
  /api/me:
    get:
      tags: [个人资料]
      summary: 获取当前用户资料
      responses:
        '200':
          description: 当前用户
          content:
            application/json:
              schema:
                type: object
                required: [user, trialExpiresAt]
                properties:
                  user: { $ref: '#/components/schemas/User' }
                  trialExpiresAt: { type: string, format: date-time, nullable: true, description: 试用会话销毁时间，非试用恒为 null }
        '401':
          description: 未认证（unauthorized）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
    patch:
      tags: [个人资料]
      summary: 更新当前用户资料
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name: { type: string }
                contacts:
                  type: array
                  items:
                    type: object
                    required: [platform, value]
                    properties:
                      platform: { type: string }
                      value: { type: string }
      responses:
        '200':
          description: 更新后的用户
          content:
            application/json:
              schema:
                type: object
                required: [user]
                properties:
                  user: { $ref: '#/components/schemas/User' }
        '401':
          description: 未认证（unauthorized）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
  /api/me/onboarded:
    post:
      tags: [个人资料]
      summary: 标记已完成新手引导
      description: 幂等：仅首次调用写入 onboardedAt，重复调用不刷新时间戳。
      responses:
        '200':
          description: 更新后的用户
          content:
            application/json:
              schema:
                type: object
                required: [user]
                properties:
                  user: { $ref: '#/components/schemas/User' }
        '401':
          description: 未认证（unauthorized）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
  /api/admin/invite-codes:
    get:
      tags: [超管后台]
      summary: 列出邀请码
      description: 需 isSuperAdmin。
      responses:
        '200':
          description: 邀请码列表
          content:
            application/json:
              schema:
                type: object
                required: [inviteCodes]
                properties:
                  inviteCodes:
                    type: array
                    items:
                      type: object
                      required: [id, code, used, usedAt, createdAt]
                      properties:
                        id: { type: string }
                        code: { type: string }
                        used: { type: boolean }
                        usedAt: { type: string, format: date-time, nullable: true }
                        createdAt: { type: string, format: date-time, nullable: true }
        '401':
          description: 未认证（unauthorized）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '403':
          description: 非超级管理员（forbidden）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
    post:
      tags: [超管后台]
      summary: 创建邀请码
      description: 需 isSuperAdmin。不传 code 则自动生成 ANON-XXXXXXXX；自定义 code 最少 6 位。
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                code: { type: string, description: 最少 6 位 }
      responses:
        '201':
          description: 创建成功
          content:
            application/json:
              schema:
                type: object
                required: [id, code]
                properties:
                  id: { type: string }
                  code: { type: string }
        '400':
          description: 参数错误（bad_request）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '401':
          description: 未认证（unauthorized）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '403':
          description: 非超级管理员（forbidden）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
  /api/invites/{token}:
    get:
      tags: [邀请接受]
      summary: 查看邀请信息
      parameters:
        - name: token
          in: path
          required: true
          schema: { type: string }
      responses:
        '200':
          description: 邀请信息
          content:
            application/json:
              schema:
                type: object
                required: [invite]
                properties:
                  invite:
                    type: object
                    required: [projectName, roleName, expiresAt, targeted]
                    properties:
                      projectName: { type: string }
                      roleName: { type: string }
                      expiresAt: { type: string, format: date-time }
                      targeted: { type: boolean, description: 是否指定用户 }
        '401':
          description: 未认证（unauthorized）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '403':
          description: 邀请指定给他人（forbidden）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '404':
          description: 邀请不存在（not_found）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '410':
          description: 邀请已用/过期（invite_gone）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
  /api/invites/{token}/accept:
    post:
      tags: [邀请接受]
      summary: 接受邀请
      description: 已加入项目则更新角色。
      parameters:
        - name: token
          in: path
          required: true
          schema: { type: string }
      responses:
        '200':
          description: 已加入
          content:
            application/json:
              schema:
                type: object
                required: [ok, projectId]
                properties:
                  ok: { type: boolean }
                  projectId: { type: string }
        '401':
          description: 未认证（unauthorized）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '403':
          description: 邀请指定给他人（forbidden）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '404':
          description: 邀请不存在（not_found）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '410':
          description: 邀请已用/过期（invite_gone）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
  /api/push/config:
    get:
      tags: [推送订阅]
      summary: 获取 Web Push 配置
      description: 返回 VAPID 公钥；未配置为 null，前端据此隐藏订阅入口。
      responses:
        '200':
          description: 推送配置
          content:
            application/json:
              schema:
                type: object
                required: [publicKey]
                properties:
                  publicKey: { type: string, nullable: true, description: VAPID 公钥 }
        '401':
          description: 未认证（unauthorized）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
  /api/push/subscription:
    post:
      tags: [推送订阅]
      summary: 订阅推送
      description: 同一用户同一 endpoint 幂等 upsert；每用户最多保留 20 条订阅，超出淘汰最旧。
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [endpoint, p256dh, auth]
              properties:
                endpoint: { type: string, description: 必须 https（或 localhost http） }
                p256dh: { type: string, description: URL-safe base64 }
                auth: { type: string, description: URL-safe base64 }
                userAgent: { type: string, description: 不超过 300 字 }
      responses:
        '200':
          description: 已订阅
          content:
            application/json:
              schema:
                type: object
                required: [ok]
                properties:
                  ok: { type: boolean }
        '400':
          description: 参数错误（bad_request：endpoint 非法 / 密钥格式不符）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '401':
          description: 未认证（unauthorized）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
    delete:
      tags: [推送订阅]
      summary: 取消推送订阅
      description: 仅删除当前用户名下的该订阅。
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [endpoint]
              properties:
                endpoint: { type: string }
      responses:
        '200':
          description: 已删除
          content:
            application/json:
              schema:
                type: object
                required: [ok, removed]
                properties:
                  ok: { type: boolean }
                  removed: { type: integer, description: 删除条数 }
        '401':
          description: 未认证（unauthorized）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
  /api/open/exchange:
    post:
      tags: [OpenAPI 密钥]
      summary: 启动令牌兑换 API 密钥
      description: 限流 60 次/15 分钟/IP。生效 scopes = 工具 scopes ∩ 用户当前角色权限；同 (user, tool) 重复兑换顶替旧密钥（旧密钥立即失效）。恒 30 天有效。
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [launchToken]
              properties:
                launchToken: { type: string, description: 自定义工具 launch 端点签发的 5 分钟启动令牌 }
      responses:
        '201':
          description: 兑换成功（apiKey 原文仅此一次返回）
          content:
            application/json:
              schema:
                type: object
                required: [apiKey, expiresAt, scopes, projectId, user]
                properties:
                  apiKey: { type: string, description: anonk_ 前缀密钥原文 }
                  expiresAt: { type: string, format: date-time }
                  scopes:
                    type: array
                    items: { type: string }
                  projectId: { type: string }
                  user:
                    type: object
                    required: [id, name]
                    properties:
                      id: { type: string }
                      name: { type: string }
        '400':
          description: 参数错误（bad_request）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '401':
          description: launchToken 无效/过期（invalid_launch_token）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '403':
          description: 非项目成员（forbidden）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '404':
          description: 工具已删除（not_found）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '429':
          description: 限流（rate_limited）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
  /api/open/me:
    get:
      tags: [OpenAPI 密钥]
      summary: 查看当前 API 密钥身份
      description: 需 API 密钥调用；permissions 为密钥 scopes 与持有者实时角色权限的交集。
      responses:
        '200':
          description: 密钥身份
          content:
            application/json:
              schema:
                type: object
                required: [user, project, permissions, expiresAt]
                properties:
                  user:
                    type: object
                    required: [id, name]
                    properties:
                      id: { type: string }
                      name: { type: string }
                  project:
                    type: object
                    required: [id, name]
                    properties:
                      id: { type: string }
                      name: { type: string }
                  permissions:
                    type: array
                    items: { type: string }
                  expiresAt: { type: string, format: date-time, nullable: true, description: 永久密钥为 null }
        '401':
          description: 未认证（unauthorized）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '403':
          description: 以用户 JWT 调用（api_key_required）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
  /api/open/keys:
    get:
      tags: [OpenAPI 密钥]
      summary: 列出本人 API 密钥
      description: 用户态接口，API 密钥不可调。仅本人密钥，createdAt 倒序。
      responses:
        '200':
          description: 密钥列表
          content:
            application/json:
              schema:
                type: object
                required: [keys]
                properties:
                  keys:
                    type: array
                    items: { $ref: '#/components/schemas/ApiKeyInfo' }
        '401':
          description: 未认证（unauthorized）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '403':
          description: API 密钥调用用户态接口（api_key_forbidden）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
    post:
      tags: [OpenAPI 密钥]
      summary: 自助生成 API 密钥
      description: 用户态接口，API 密钥不可调。scopes 静默收窄为「合法且用户实际持有」的子集；permanent=true 永久有效，否则 30 天。
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [projectId, name]
              properties:
                projectId: { type: string }
                name: { type: string, description: 必填，不超过 50 字 }
                scopes:
                  type: array
                  items: { type: string }
                permanent: { type: boolean }
      responses:
        '201':
          description: 创建成功（apiKey 原文仅此一次返回）
          content:
            application/json:
              schema:
                type: object
                required: [apiKey, key]
                properties:
                  apiKey: { type: string, description: anonk_ 前缀密钥原文 }
                  key: { $ref: '#/components/schemas/ApiKeyInfo' }
        '400':
          description: 参数错误（bad_request）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '401':
          description: 未认证（unauthorized）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '403':
          description: 非项目成员（forbidden）或 API 密钥调用用户态接口（api_key_forbidden）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
  /api/open/keys/{keyId}:
    delete:
      tags: [OpenAPI 密钥]
      summary: 撤销 API 密钥
      description: 用户态接口，API 密钥不可调。仅可撤销本人密钥；硬删除，撤销后该密钥立即 401。
      parameters:
        - name: keyId
          in: path
          required: true
          schema: { type: string }
      responses:
        '200':
          description: 已撤销
          content:
            application/json:
              schema:
                type: object
                required: [ok]
                properties:
                  ok: { type: boolean }
        '401':
          description: 未认证（unauthorized）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '403':
          description: API 密钥调用用户态接口（api_key_forbidden）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '404':
          description: 密钥不存在或属于他人（not_found）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
  /api/projects:
    post:
      tags: [项目]
      summary: 创建项目
      description: 仅接受用户 JWT（API 密钥调用返回 403 api_key_forbidden）。创建者自动成为成员，角色为「主办」。
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [name]
              properties:
                name: { type: string, description: 项目名称（必填） }
                description: { type: string }
                startDate: { type: string, format: date-time }
                endDate: { type: string, format: date-time }
      responses:
        '201':
          description: 创建成功
          content:
            application/json:
              schema:
                type: object
                required: [project]
                properties:
                  project: { $ref: '#/components/schemas/ProjectDetail' }
        '400':
          description: 参数错误（bad_request，项目名称必填）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '401':
          description: 未认证（unauthorized）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '403':
          description: 禁止访问（api_key_forbidden）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
    get:
      tags: [项目]
      summary: 列出我参与的项目
      description: 仅接受用户 JWT（API 密钥调用返回 403 api_key_forbidden）。仅返回当前用户参与的项目。
      responses:
        '200':
          description: 项目列表
          content:
            application/json:
              schema:
                type: object
                required: [projects]
                properties:
                  projects:
                    type: array
                    items: { $ref: '#/components/schemas/ProjectSummary' }
        '401':
          description: 未认证（unauthorized）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '403':
          description: 禁止访问（api_key_forbidden）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
  /api/projects/{id}:
    get:
      tags: [项目]
      summary: 获取项目详情
      description: 项目成员或超管可访问。超管的 myRole 固定为「超级管理员」。
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
      responses:
        '200':
          description: 项目详情与成员列表
          content:
            application/json:
              schema:
                type: object
                required: [project, members, myRole, myPermissions]
                properties:
                  project: { $ref: '#/components/schemas/ProjectDetail' }
                  members:
                    type: array
                    items: { $ref: '#/components/schemas/Member' }
                  myRole: { type: string }
                  myPermissions:
                    type: array
                    items: { type: string }
        '401':
          description: 未认证（unauthorized）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '403':
          description: 禁止访问（forbidden / api_key_wrong_project）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '404':
          description: 项目不存在（not_found）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
    patch:
      tags: [项目]
      summary: 更新项目信息
      description: 需 project:manage 权限。全部字段可选；startDate/endDate 传空值清除。
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name: { type: string }
                description: { type: string }
                startDate: { type: string, format: date-time, nullable: true }
                endDate: { type: string, format: date-time, nullable: true }
                status: { $ref: '#/components/schemas/ProjectStatus' }
                location: { type: string }
                timezone: { type: string }
                currentStage: { type: string }
      responses:
        '200':
          description: 更新后的项目
          content:
            application/json:
              schema:
                type: object
                required: [project]
                properties:
                  project: { $ref: '#/components/schemas/ProjectDetail' }
        '400':
          description: 参数错误（bad_request）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '401':
          description: 未认证（unauthorized）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '403':
          description: 禁止访问（forbidden / api_key_wrong_project）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '404':
          description: 项目不存在（not_found）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
  /api/projects/{id}/roles:
    post:
      tags: [项目]
      summary: 创建角色
      description: 需 role:manage 权限。
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [name, permissions]
              properties:
                name: { type: string }
                permissions:
                  type: array
                  items: { type: string }
      responses:
        '201':
          description: 创建成功，返回全量角色列表
          content:
            application/json:
              schema:
                type: object
                required: [roles]
                properties:
                  roles:
                    type: array
                    items: { $ref: '#/components/schemas/Role' }
        '400':
          description: 参数错误（bad_request，含未知权限点）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '401':
          description: 未认证（unauthorized）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '403':
          description: 禁止访问（forbidden / api_key_wrong_project）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '404':
          description: 项目不存在（not_found）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '409':
          description: 角色已存在（role_exists）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
  /api/projects/{id}/roles/{roleName}:
    patch:
      tags: [项目]
      summary: 更新角色权限
      description: 需 role:manage 权限。roleName 需 URL 编码。
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
        - name: roleName
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [permissions]
              properties:
                permissions:
                  type: array
                  items: { type: string }
      responses:
        '200':
          description: 更新成功，返回全量角色列表
          content:
            application/json:
              schema:
                type: object
                required: [roles]
                properties:
                  roles:
                    type: array
                    items: { $ref: '#/components/schemas/Role' }
        '400':
          description: 参数错误（bad_request，含未知权限点）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '401':
          description: 未认证（unauthorized）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '403':
          description: 禁止访问（forbidden / api_key_wrong_project）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '404':
          description: 项目或角色不存在（not_found）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
    delete:
      tags: [项目]
      summary: 删除角色
      description: 需 role:manage 权限。仍有成员使用时返回 409。
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
        - name: roleName
          in: path
          required: true
          schema: { type: string }
      responses:
        '200':
          description: 删除成功，返回全量角色列表
          content:
            application/json:
              schema:
                type: object
                required: [roles]
                properties:
                  roles:
                    type: array
                    items: { $ref: '#/components/schemas/Role' }
        '401':
          description: 未认证（unauthorized）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '403':
          description: 禁止访问（forbidden / api_key_wrong_project）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '404':
          description: 项目或角色不存在（not_found）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '409':
          description: 仍有成员使用该角色（role_in_use）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
  /api/projects/{id}/members/{userId}:
    patch:
      tags: [项目]
      summary: 修改成员角色
      description: 需 member:manage 权限。
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
        - name: userId
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [roleName]
              properties:
                roleName: { type: string }
      responses:
        '200':
          description: 修改成功，返回全量成员列表
          content:
            application/json:
              schema:
                type: object
                required: [members]
                properties:
                  members:
                    type: array
                    items: { $ref: '#/components/schemas/Member' }
        '400':
          description: 参数错误（bad_request，角色不存在）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '401':
          description: 未认证（unauthorized）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '403':
          description: 禁止访问（forbidden / api_key_wrong_project）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '404':
          description: 项目或成员不存在（not_found）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
    delete:
      tags: [项目]
      summary: 移除成员
      description: 需 member:manage 权限。不能移除自己。
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
        - name: userId
          in: path
          required: true
          schema: { type: string }
      responses:
        '200':
          description: 移除成功，返回全量成员列表
          content:
            application/json:
              schema:
                type: object
                required: [members]
                properties:
                  members:
                    type: array
                    items: { $ref: '#/components/schemas/Member' }
        '400':
          description: 参数错误（bad_request，不能移除自己）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '401':
          description: 未认证（unauthorized）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '403':
          description: 禁止访问（forbidden / api_key_wrong_project）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '404':
          description: 项目或成员不存在（not_found）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
  /api/projects/{id}/invites:
    post:
      tags: [项目]
      summary: 创建项目邀请
      description: 需 member:manage 权限。不传 targetUserId 为开放链接（任何登录用户可接受，一次性）；传入则仅该用户可接受。前端拼 location.origin + url 发给对方。
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [roleName]
              properties:
                roleName: { type: string }
                targetUserId: { type: string, description: 限定接受人（可选） }
                expiresInHours: { type: integer, description: 有效期小时数，默认 72，合法范围 1~720 }
      responses:
        '201':
          description: 创建成功
          content:
            application/json:
              schema:
                type: object
                required: [token, url]
                properties:
                  token: { type: string }
                  url: { type: string, description: 形如 /invite/<token> 的相对路径 }
        '400':
          description: 参数错误（bad_request，角色不存在/目标用户不存在/有效期越界）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '401':
          description: 未认证（unauthorized）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '403':
          description: 禁止访问（forbidden / api_key_wrong_project）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '404':
          description: 项目不存在（not_found）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
  /api/projects/{id}/stages:
    get:
      tags: [项目]
      summary: 列出项目阶段
      description: 项目成员可访问。stages 按 order 升序；currentStageIndex 为第一个未完成阶段的下标，全部完成为 -1。
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
      responses:
        '200':
          description: 阶段列表
          content:
            application/json:
              schema:
                type: object
                required: [stages, currentStageIndex]
                properties:
                  stages:
                    type: array
                    items: { $ref: '#/components/schemas/StageItem' }
                  currentStageIndex: { type: integer, description: 第一个未完成阶段下标；全部完成为 -1 }
        '401':
          description: 未认证（unauthorized）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '403':
          description: 禁止访问（forbidden / api_key_wrong_project）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '404':
          description: 项目不存在（not_found）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
    post:
      tags: [项目]
      summary: 新增项目阶段
      description: 需 project:manage 权限。不传 order 追加到末尾。返回形态同 GET。
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [name]
              properties:
                name: { type: string }
                order: { type: integer }
      responses:
        '200':
          description: 新增成功，返回阶段列表
          content:
            application/json:
              schema:
                type: object
                required: [stages, currentStageIndex]
                properties:
                  stages:
                    type: array
                    items: { $ref: '#/components/schemas/StageItem' }
                  currentStageIndex: { type: integer }
        '400':
          description: 参数错误（bad_request，名称为空）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '401':
          description: 未认证（unauthorized）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '403':
          description: 禁止访问（forbidden / api_key_wrong_project）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '404':
          description: 项目不存在（not_found）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
  /api/projects/{id}/stages/reorder:
    patch:
      tags: [项目]
      summary: 调整阶段顺序
      description: 需 project:manage 权限。orderedIds 为全量阶段 id，按新顺序排列。返回形态同 GET 阶段列表。
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [orderedIds]
              properties:
                orderedIds:
                  type: array
                  items: { type: string }
      responses:
        '200':
          description: 调整成功，返回阶段列表
          content:
            application/json:
              schema:
                type: object
                required: [stages, currentStageIndex]
                properties:
                  stages:
                    type: array
                    items: { $ref: '#/components/schemas/StageItem' }
                  currentStageIndex: { type: integer }
        '400':
          description: 参数错误（bad_request，orderedIds 须为非空数组）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '401':
          description: 未认证（unauthorized）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '403':
          description: 禁止访问（forbidden / api_key_wrong_project）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '404':
          description: 项目不存在（not_found）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
  /api/projects/{id}/stages/{stageId}:
    patch:
      tags: [项目]
      summary: 更新项目阶段
      description: 需 project:manage 权限。completedAt 传 null 取消完成；标记完成即推进「当前阶段」（由完成态推导，无独立切换端点）。返回形态同 GET 阶段列表。
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
        - name: stageId
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                completedAt: { type: string, format: date-time, nullable: true }
                note: { type: string }
      responses:
        '200':
          description: 更新成功，返回阶段列表
          content:
            application/json:
              schema:
                type: object
                required: [stages, currentStageIndex]
                properties:
                  stages:
                    type: array
                    items: { $ref: '#/components/schemas/StageItem' }
                  currentStageIndex: { type: integer }
        '401':
          description: 未认证（unauthorized）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '403':
          description: 禁止访问（forbidden / api_key_wrong_project）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '404':
          description: 项目或阶段不存在（not_found）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
    delete:
      tags: [项目]
      summary: 删除项目阶段
      description: 需 project:manage 权限。删除后关联里程碑自动改为不关联阶段。返回形态同 GET 阶段列表。
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
        - name: stageId
          in: path
          required: true
          schema: { type: string }
      responses:
        '200':
          description: 删除成功，返回阶段列表
          content:
            application/json:
              schema:
                type: object
                required: [stages, currentStageIndex]
                properties:
                  stages:
                    type: array
                    items: { $ref: '#/components/schemas/StageItem' }
                  currentStageIndex: { type: integer }
        '401':
          description: 未认证（unauthorized）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '403':
          description: 禁止访问（forbidden / api_key_wrong_project）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '404':
          description: 项目或阶段不存在（not_found）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '409':
          description: 冲突（conflict，至少需要保留一个阶段）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
  /api/projects/{id}/milestones:
    get:
      tags: [里程碑]
      summary: 列出里程碑
      description: 需项目成员身份。按 date 升序。
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
        - name: from
          in: query
          schema: { type: string, format: date-time }
          description: 起始时间（按 date 闭区间过滤）
        - name: to
          in: query
          schema: { type: string, format: date-time }
          description: 截止时间（按 date 闭区间过滤）
      responses:
        '200':
          description: 里程碑列表
          content:
            application/json:
              schema:
                type: object
                required: [milestones]
                properties:
                  milestones:
                    type: array
                    items: { $ref: '#/components/schemas/MilestoneItem' }
        '401':
          description: 未认证（unauthorized）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '403':
          description: 禁止访问（forbidden / api_key_wrong_project）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '404':
          description: 项目不存在（not_found）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
    post:
      tags: [里程碑]
      summary: 创建里程碑
      description: 需 project:manage 权限。
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [title, date]
              properties:
                title: { type: string }
                date: { type: string, format: date-time }
                description: { type: string }
                stageId: { type: string, description: 关联的项目阶段 id（可选） }
      responses:
        '201':
          description: 创建成功
          content:
            application/json:
              schema:
                type: object
                required: [milestone]
                properties:
                  milestone: { $ref: '#/components/schemas/MilestoneItem' }
        '400':
          description: 参数错误（bad_request，标题/日期为空或日期格式无效）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '401':
          description: 未认证（unauthorized）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '403':
          description: 禁止访问（forbidden / api_key_wrong_project）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '404':
          description: 项目不存在（not_found）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
  /api/projects/{id}/milestones/{milestoneId}:
    patch:
      tags: [里程碑]
      summary: 更新里程碑
      description: 需 project:manage 权限。字段同创建（均可选），另可加 completedAt（传 null 取消完成）。
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
        - name: milestoneId
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                title: { type: string }
                date: { type: string, format: date-time }
                description: { type: string }
                stageId: { type: string, nullable: true }
                completedAt: { type: string, format: date-time, nullable: true }
      responses:
        '200':
          description: 更新成功
          content:
            application/json:
              schema:
                type: object
                required: [milestone]
                properties:
                  milestone: { $ref: '#/components/schemas/MilestoneItem' }
        '400':
          description: 参数错误（bad_request，日期格式无效）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '401':
          description: 未认证（unauthorized）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '403':
          description: 禁止访问（forbidden / api_key_wrong_project）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '404':
          description: 项目或里程碑不存在（not_found，含跨项目）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
    delete:
      tags: [里程碑]
      summary: 删除里程碑
      description: 需 project:manage 权限。
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
        - name: milestoneId
          in: path
          required: true
          schema: { type: string }
      responses:
        '200':
          description: 删除成功
          content:
            application/json:
              schema:
                type: object
                required: [ok]
                properties:
                  ok: { type: boolean }
        '401':
          description: 未认证（unauthorized）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '403':
          description: 禁止访问（forbidden / api_key_wrong_project）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '404':
          description: 项目或里程碑不存在（not_found）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
  /api/projects/{id}/milestones/{milestoneId}/complete:
    post:
      tags: [里程碑]
      summary: 标记里程碑完成
      description: 需 project:manage 权限。completedAt 置为当前时间；重复调用会刷新完成时间。
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
        - name: milestoneId
          in: path
          required: true
          schema: { type: string }
      responses:
        '200':
          description: 标记成功
          content:
            application/json:
              schema:
                type: object
                required: [milestone]
                properties:
                  milestone: { $ref: '#/components/schemas/MilestoneItem' }
        '401':
          description: 未认证（unauthorized）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '403':
          description: 禁止访问（forbidden / api_key_wrong_project）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '404':
          description: 项目或里程碑不存在（not_found）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
  /api/projects/{id}/todos:
    get:
      tags: [待办]
      summary: 列出待办
      description: 需项目成员身份。支持按分类/指派人/状态过滤与排序。
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
        - name: category
          in: query
          schema: { type: string }
          description: 按分类过滤
        - name: assignee
          in: query
          schema: { type: string }
          description: 按指派人 userId 过滤
        - name: status
          in: query
          schema: { type: string, enum: [open, done] }
          description: 按状态过滤
        - name: sort
          in: query
          schema: { type: string, enum: [createdAt, dueAt, nodeAt], default: createdAt }
          description: 排序字段
        - name: order
          in: query
          schema: { type: string, enum: [asc, desc], default: desc }
          description: 排序方向
      responses:
        '200':
          description: 待办列表
          content:
            application/json:
              schema:
                type: object
                required: [todos]
                properties:
                  todos:
                    type: array
                    items: { $ref: '#/components/schemas/TodoItem' }
        '401':
          description: 未认证（unauthorized）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
    post:
      tags: [待办]
      summary: 创建待办
      description: 需 todo:create 权限（project:manage 等价放行）。
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [title]
              properties:
                title: { type: string }
                category: { type: string }
                assigneeIds:
                  type: array
                  items: { type: string }
                  description: 指派人 userId 列表，必须全部为项目成员
                nodeAt: { type: string, format: date-time }
                dueAt: { type: string, format: date-time }
                remindAt: { type: string, format: date-time }
                note: { type: string }
      responses:
        '201':
          description: 创建成功
          content:
            application/json:
              schema:
                type: object
                required: [todo]
                properties:
                  todo: { $ref: '#/components/schemas/TodoItem' }
        '400':
          description: 参数错误（bad_request：标题必填 / 指派人必须是项目成员 / 时间格式无效）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '401':
          description: 未认证（unauthorized）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '403':
          description: 无 todo:create 权限（forbidden / api_key_forbidden / api_key_wrong_project）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
  /api/projects/{id}/todos/{todoId}:
    patch:
      tags: [待办]
      summary: 更新待办
      description: 需 todo:manage 权限。字段均可选；status 置 open 会清除完成信息。
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
        - name: todoId
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                title: { type: string }
                category: { type: string }
                assigneeIds:
                  type: array
                  items: { type: string }
                nodeAt: { type: string, format: date-time }
                dueAt: { type: string, format: date-time }
                remindAt: { type: string, format: date-time }
                note: { type: string }
                status: { type: string, enum: [open, done] }
      responses:
        '200':
          description: 更新成功
          content:
            application/json:
              schema:
                type: object
                required: [todo]
                properties:
                  todo: { $ref: '#/components/schemas/TodoItem' }
        '400':
          description: 参数错误（bad_request：status 无效 / 时间格式无效 / 指派人必须是项目成员）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '401':
          description: 未认证（unauthorized）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '403':
          description: 无 todo:manage 权限（forbidden / api_key_forbidden / api_key_wrong_project）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '404':
          description: 待办不存在（not_found）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
    delete:
      tags: [待办]
      summary: 删除待办
      description: 需 todo:manage 权限。
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
        - name: todoId
          in: path
          required: true
          schema: { type: string }
      responses:
        '200':
          description: 删除成功
          content:
            application/json:
              schema:
                type: object
                required: [ok]
                properties:
                  ok: { type: boolean }
        '401':
          description: 未认证（unauthorized）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '403':
          description: 无 todo:manage 权限（forbidden / api_key_forbidden / api_key_wrong_project）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '404':
          description: 待办不存在（not_found）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
  /api/projects/{id}/todos/{todoId}/complete:
    post:
      tags: [待办]
      summary: 完成待办
      description: 需 todo:manage，或「当前用户是指派人且有 todo:complete」。置 status=done，记录完成人/时间/备注，文件入附件。
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
        - name: todoId
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: false
        content:
          multipart/form-data:
            schema:
              type: object
              properties:
                completionNote: { type: string, description: 完成备注（可选） }
                files:
                  type: array
                  items: { type: string, format: binary }
                  description: 附件，可多文件，每个 ≤20MB
      responses:
        '200':
          description: 完成成功
          content:
            application/json:
              schema:
                type: object
                required: [todo]
                properties:
                  todo: { $ref: '#/components/schemas/TodoItem' }
        '401':
          description: 未认证（unauthorized）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '403':
          description: 没有权限完成该待办（forbidden / api_key_forbidden / api_key_wrong_project）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '404':
          description: 待办不存在（not_found）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '409':
          description: 待办已完成（already_done）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
  /api/projects/{id}/todos/{todoId}/updates:
    post:
      tags: [待办]
      summary: 追加待办进度
      description: 权限同完成端点。向待办追加一条进度（不可编辑/删除）；备注与附件至少其一非空。
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
        - name: todoId
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              properties:
                note: { type: string, description: 进度备注（与附件至少其一非空） }
                files:
                  type: array
                  items: { type: string, format: binary }
                  description: 附件，可多文件，每个 ≤20MB
      responses:
        '201':
          description: 追加成功
          content:
            application/json:
              schema:
                type: object
                required: [todo]
                properties:
                  todo: { $ref: '#/components/schemas/TodoItem' }
        '400':
          description: 进度内容为空（bad_request）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '401':
          description: 未认证（unauthorized）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '403':
          description: 没有权限（forbidden / api_key_forbidden / api_key_wrong_project）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '404':
          description: 待办不存在（not_found）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '409':
          description: 待办已完成（already_done）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
  /api/projects/{id}/todos/template/export:
    get:
      tags: [待办]
      summary: 导出待办模板
      description: 需项目成员身份。offset = 原时间字段 − 项目锚点时间（毫秒，可为负）。
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
      responses:
        '200':
          description: 模板 JSON 对象
          content:
            application/json:
              schema:
                type: object
                required: [name, exportedAt, anchorField, anchorDate, todos]
                properties:
                  name: { type: string, description: 形如「项目名 待办模板」 }
                  exportedAt: { type: string, format: date-time }
                  anchorField: { type: string, enum: [start, end, export], description: 锚点来源字段 }
                  anchorDate: { type: string, format: date-time }
                  todos:
                    type: array
                    items:
                      type: object
                      required: [title, nodeOffsetMs, dueOffsetMs, remindOffsetMs]
                      properties:
                        title: { type: string }
                        category: { type: string }
                        note: { type: string }
                        nodeOffsetMs: { type: integer, nullable: true, description: 相对锚点的毫秒偏移，可为负 }
                        dueOffsetMs: { type: integer, nullable: true }
                        remindOffsetMs: { type: integer, nullable: true }
        '401':
          description: 未认证（unauthorized）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
  /api/projects/{id}/todos/template/import:
    post:
      tags: [待办]
      summary: 导入待办模板
      description: 需 todo:manage 权限。以 date 为新锚点批量生成待办（无指派人）。
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [template, anchor, date]
              properties:
                template: { type: object, description: 导出的模板 JSON 对象 }
                anchor: { type: string, enum: [start, end] }
                date: { type: string, description: 新锚点日期，如 2026-10-01 }
      responses:
        '201':
          description: 导入成功
          content:
            application/json:
              schema:
                type: object
                required: [created]
                properties:
                  created: { type: integer, description: 生成的待办数量 }
        '400':
          description: 参数错误（bad_request：模板格式无效 / anchor 必须是 start 或 end / 锚定日期无效）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '401':
          description: 未认证（unauthorized）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '403':
          description: 无 todo:manage 权限（forbidden / api_key_forbidden / api_key_wrong_project）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
  /api/projects/{id}/announcements:
    get:
      tags: [公告]
      summary: 列出公告
      description: 需项目成员身份。置顶优先，其后按发布时间倒序；按当前用户可见范围过滤，total 为可见总数。
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
        - name: page
          in: query
          schema: { type: integer, default: 1 }
          description: 页码
        - name: limit
          in: query
          schema: { type: integer, default: 20, maximum: 50 }
          description: 每页数量，上限 50
        - name: includeExpired
          in: query
          schema: { type: boolean }
          description: 传 true 时包含已过期公告（默认排除）
      responses:
        '200':
          description: 公告分页列表
          content:
            application/json:
              schema:
                type: object
                required: [announcements, total, page]
                properties:
                  announcements:
                    type: array
                    items: { $ref: '#/components/schemas/AnnouncementItem' }
                  total: { type: integer, description: 当前用户可见总数 }
                  page: { type: integer }
        '401':
          description: 未认证（unauthorized）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
    post:
      tags: [公告]
      summary: 发布公告
      description: 需 announcement:manage 权限（project:manage 等价放行）。重要/紧急公告发布时向可见范围内成员发通知。
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [title]
              properties:
                title: { type: string }
                content: { type: string }
                type: { type: string, enum: [normal, important, emergency] }
                isPinned: { type: boolean }
                requireConfirmation: { type: boolean }
                visibility:
                  $ref: '#/components/schemas/Visibility'
                expiresAt: { type: string, format: date-time, nullable: true }
      responses:
        '201':
          description: 发布成功
          content:
            application/json:
              schema:
                type: object
                required: [announcement]
                properties:
                  announcement:
                    type: object
                    required: [id, title]
                    properties:
                      id: { type: string }
                      title: { type: string }
        '400':
          description: 参数错误（bad_request：标题必填 / 公告类型无效 / 截止时间无效）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '401':
          description: 未认证（unauthorized）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '403':
          description: 无 announcement:manage 权限（forbidden / api_key_forbidden / api_key_wrong_project）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
  /api/projects/{id}/announcements/{announcementId}:
    patch:
      tags: [公告]
      summary: 更新公告
      description: 需 announcement:manage 权限。字段均可选；expiresAt 传 null 清除过期时间。
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
        - name: announcementId
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                title: { type: string }
                content: { type: string }
                type: { type: string, enum: [normal, important, emergency] }
                isPinned: { type: boolean }
                requireConfirmation: { type: boolean }
                visibility:
                  $ref: '#/components/schemas/Visibility'
                expiresAt: { type: string, format: date-time, nullable: true }
      responses:
        '200':
          description: 更新成功
          content:
            application/json:
              schema:
                type: object
                required: [ok]
                properties:
                  ok: { type: boolean }
        '400':
          description: 参数错误（bad_request：公告类型无效 / 截止时间无效）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '401':
          description: 未认证（unauthorized）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '403':
          description: 无 announcement:manage 权限（forbidden / api_key_forbidden / api_key_wrong_project）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '404':
          description: 公告不存在（not_found）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
    delete:
      tags: [公告]
      summary: 删除公告
      description: 需 announcement:manage 权限。同时删除全部确认记录。
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
        - name: announcementId
          in: path
          required: true
          schema: { type: string }
      responses:
        '200':
          description: 删除成功
          content:
            application/json:
              schema:
                type: object
                required: [ok]
                properties:
                  ok: { type: boolean }
        '401':
          description: 未认证（unauthorized）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '403':
          description: 无 announcement:manage 权限（forbidden / api_key_forbidden / api_key_wrong_project）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '404':
          description: 公告不存在（not_found）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
  /api/projects/{id}/announcements/{announcementId}/confirm:
    post:
      tags: [公告]
      summary: 确认知悉公告
      description: 需项目成员身份且在公告可见范围内。「我已知悉」确认，幂等不刷新时间戳。
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
        - name: announcementId
          in: path
          required: true
          schema: { type: string }
      responses:
        '200':
          description: 确认成功
          content:
            application/json:
              schema:
                type: object
                required: [ok, confirmedAt]
                properties:
                  ok: { type: boolean }
                  confirmedAt: { type: string, format: date-time }
        '401':
          description: 未认证（unauthorized）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '403':
          description: 无权查看此公告（forbidden）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '404':
          description: 公告不存在（not_found）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
  /api/projects/{id}/announcements/{announcementId}/confirmations:
    get:
      tags: [公告]
      summary: 查看公告确认情况
      description: 需 announcement:manage 权限。按项目全体成员拆分为已确认/未确认两组。
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
        - name: announcementId
          in: path
          required: true
          schema: { type: string }
      responses:
        '200':
          description: 确认名单
          content:
            application/json:
              schema:
                type: object
                required: [confirmed, unconfirmed]
                properties:
                  confirmed:
                    type: array
                    items:
                      type: object
                      required: [userId, name]
                      properties:
                        userId: { type: string }
                        name: { type: string }
                  unconfirmed:
                    type: array
                    items:
                      type: object
                      required: [userId, name]
                      properties:
                        userId: { type: string }
                        name: { type: string }
        '401':
          description: 未认证（unauthorized）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '403':
          description: 无 announcement:manage 权限（forbidden / api_key_forbidden / api_key_wrong_project）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '404':
          description: 公告不存在（not_found）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
  /api/projects/{id}/dashboard:
    get:
      tags: [工作台仪表盘]
      summary: 获取工作台聚合数据
      description: 需项目成员身份。聚合首屏数据，按权限裁剪（无财务权限点时 summary.modules.finance 为 null）。
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
        - name: scheduleDays
          in: query
          schema: { type: integer, maximum: 30 }
          description: 日程天数；缺省用偏好的 scheduleRange，上限 30
      responses:
        '200':
          description: 聚合首屏数据
          content:
            application/json:
              schema:
                type: object
                properties:
                  summary: { $ref: '#/components/schemas/DashboardSummary' }
                  myActions:
                    type: object
                    properties:
                      items:
                        type: array
                        items: { $ref: '#/components/schemas/DashboardActionItem' }
                  risks:
                    type: object
                    properties:
                      risks:
                        type: array
                        description: 精简字段（不含 resolvedAt 与忽略字段）
                        items: { $ref: '#/components/schemas/RiskItem' }
                      health: { $ref: '#/components/schemas/HealthStatus' }
                  schedule:
                    type: object
                    properties:
                      groups:
                        type: array
                        items: { $ref: '#/components/schemas/ScheduleGroup' }
                  announcements:
                    type: object
                    properties:
                      items:
                        type: array
                        description: 当前用户可见，至多 5 条
                        items: { $ref: '#/components/schemas/AnnouncementItem' }
                  activities:
                    type: object
                    properties:
                      items:
                        type: array
                        description: 至多 10 条
                        items: { $ref: '#/components/schemas/ActivityItem' }
                  preferences: { $ref: '#/components/schemas/DashboardPreferences' }
        '401':
          description: 未认证（unauthorized）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
  /api/projects/{id}/dashboard/summary:
    get:
      tags: [工作台仪表盘]
      summary: 获取仪表盘汇总
      description: 需项目成员身份。
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
      responses:
        '200':
          description: 汇总指标与模块概览
          content:
            application/json:
              schema: { $ref: '#/components/schemas/DashboardSummary' }
        '401':
          description: 未认证（unauthorized）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
  /api/projects/{id}/dashboard/my-actions:
    get:
      tags: [工作台仪表盘]
      summary: 获取本人待行动项
      description: 需项目成员身份。指派给本人的未完成待办 + 未确认的现场任务；逾期优先，再按 dueAt 升序，无日期排最后。
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
      responses:
        '200':
          description: 待行动项列表
          content:
            application/json:
              schema:
                type: object
                required: [items]
                properties:
                  items:
                    type: array
                    items: { $ref: '#/components/schemas/DashboardActionItem' }
        '401':
          description: 未认证（unauthorized）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
  /api/projects/{id}/dashboard/schedule:
    get:
      tags: [工作台仪表盘]
      summary: 获取日程分组
      description: 需项目成员身份。
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
        - name: days
          in: query
          schema: { type: integer, default: 7, maximum: 30 }
          description: 日程天数，上限 30
      responses:
        '200':
          description: 日程分组列表
          content:
            application/json:
              schema:
                type: object
                required: [groups]
                properties:
                  groups:
                    type: array
                    items: { $ref: '#/components/schemas/ScheduleGroup' }
        '401':
          description: 未认证（unauthorized）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
  /api/projects/{id}/dashboard/preferences:
    get:
      tags: [工作台仪表盘]
      summary: 获取工作台偏好
      description: 需项目成员身份。无记录时返回默认值（defaultView 依权限默认为 project/personal）。
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
      responses:
        '200':
          description: 工作台偏好
          content:
            application/json:
              schema: { $ref: '#/components/schemas/DashboardPreferences' }
        '401':
          description: 未认证（unauthorized）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
    patch:
      tags: [工作台仪表盘]
      summary: 更新工作台偏好
      description: 需项目成员身份。字段均可选；卡片数组各截断至 20 条；幂等 upsert。
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                defaultView: { type: string, enum: [personal, project] }
                collapsedCards:
                  type: array
                  items: { type: string }
                hiddenCards:
                  type: array
                  items: { type: string }
                scheduleRange: { type: integer, enum: [7, 30] }
                cardOrder:
                  type: array
                  items: { type: string }
      responses:
        '200':
          description: 更新后的偏好
          content:
            application/json:
              schema: { $ref: '#/components/schemas/DashboardPreferences' }
        '400':
          description: 参数错误（bad_request：defaultView 无效 / 卡片字段必须是数组 / scheduleRange 必须是 7 或 30）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '401':
          description: 未认证（unauthorized）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
  /api/projects/{id}/activities:
    get:
      tags: [项目动态]
      summary: 列出项目动态
      description: 需项目成员身份。按创建时间倒序；带权限门（permissionGate）的动态仅对拥有该权限点（或 project:manage）的成员可见。
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
        - name: limit
          in: query
          schema: { type: integer, default: 30, maximum: 50 }
          description: 数量上限 50
        - name: before
          in: query
          schema: { type: string, format: date-time }
          description: 取早于该时间的动态（翻页游标）
        - name: sourceType
          in: query
          schema: { type: string }
          description: 按来源类型过滤
      responses:
        '200':
          description: 动态时间线
          content:
            application/json:
              schema:
                type: object
                required: [activities, hasMore]
                properties:
                  activities:
                    type: array
                    items: { $ref: '#/components/schemas/ActivityItem' }
                  hasMore: { type: boolean }
        '401':
          description: 未认证（unauthorized）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
  /api/projects/{id}/risks:
    get:
      tags: [风险预警]
      summary: 列出风险预警
      description: 需项目成员身份。仅返回 active 与 ignored 状态；排序为级别 critical→warning→info，同级按最近检测时间倒序。
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
      responses:
        '200':
          description: 风险列表与健康度
          content:
            application/json:
              schema:
                type: object
                required: [risks, health]
                properties:
                  risks:
                    type: array
                    items: { $ref: '#/components/schemas/RiskItem' }
                  health: { $ref: '#/components/schemas/HealthStatus' }
        '401':
          description: 未认证（unauthorized）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
  /api/projects/{id}/risks/evaluate:
    post:
      tags: [风险预警]
      summary: 立即重算风险
      description: 需项目成员身份。立即重算全部风险规则后返回，响应同列表接口。
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
      responses:
        '200':
          description: 重算后的风险列表与健康度
          content:
            application/json:
              schema:
                type: object
                required: [risks, health]
                properties:
                  risks:
                    type: array
                    items: { $ref: '#/components/schemas/RiskItem' }
                  health: { $ref: '#/components/schemas/HealthStatus' }
        '401':
          description: 未认证（unauthorized）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
  /api/projects/{id}/risks/{riskId}/ignore:
    post:
      tags: [风险预警]
      summary: 忽略风险
      description: 需 project:manage 权限。仅可忽略 active 状态的风险。
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
        - name: riskId
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [reason]
              properties:
                reason: { type: string, description: 忽略原因（必填） }
                ignoredUntil: { type: string, format: date-time, description: 忽略截止时间（可选） }
      responses:
        '200':
          description: 忽略成功
          content:
            application/json:
              schema:
                type: object
                required: [risk]
                properties:
                  risk: { $ref: '#/components/schemas/RiskItem' }
        '400':
          description: 参数错误（bad_request：忽略原因必填 / 只能忽略生效中的风险）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '401':
          description: 未认证（unauthorized）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '403':
          description: 无 project:manage 权限（forbidden / api_key_forbidden / api_key_wrong_project）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '404':
          description: 风险不存在（not_found）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
  /api/projects/{id}/risks/{riskId}/restore:
    post:
      tags: [风险预警]
      summary: 恢复风险
      description: 需 project:manage 权限。恢复已忽略的风险为 active（清除忽略人/期限/原因）。
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
        - name: riskId
          in: path
          required: true
          schema: { type: string }
      responses:
        '200':
          description: 恢复成功
          content:
            application/json:
              schema:
                type: object
                required: [risk]
                properties:
                  risk: { $ref: '#/components/schemas/RiskItem' }
        '400':
          description: 参数错误（bad_request：只能恢复已忽略的风险）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '401':
          description: 未认证（unauthorized）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '403':
          description: 无 project:manage 权限（forbidden / api_key_forbidden / api_key_wrong_project）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '404':
          description: 风险不存在（not_found）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
  /api/cron/reminders:
    post:
      tags: [定时提醒]
      summary: 扫描发送到期提醒
      description: 运维端点。扫描所有 status=open 且 remindAt/dueAt 已到期的待办通知指派人，同时扫描 3 天内到期的未完成里程碑通知项目管理者；每条只发一次（ReminderLog 去重）。
      security: [{ cronSecret: [] }]
      responses:
        '200':
          description: 发送成功
          content:
            application/json:
              schema:
                type: object
                required: [sent]
                properties:
                  sent: { type: integer, description: 实际投递的提醒条数 }
        '401':
          description: 密钥错误（unauthorized）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '503':
          description: 未配置 CRON_SECRET（cron_disabled）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
  /api/cron/weekly-report:
    post:
      tags: [定时提醒]
      summary: 发送项目周报
      description: 运维端点。向活跃项目的管理者发送周报（本周完成待办数、新增风险数、当前阶段进度、下周里程碑）；每项目每周只发一次（WeeklyReportLog 去重）。
      security: [{ cronSecret: [] }]
      responses:
        '200':
          description: 发送成功
          content:
            application/json:
              schema:
                type: object
                required: [sent]
                properties:
                  sent: { type: integer, description: 实际投递的周报条数 }
        '401':
          description: 密钥错误（unauthorized）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '503':
          description: 未配置 CRON_SECRET（cron_disabled）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
  /api/projects/{id}/files:
    post:
      tags: [文件]
      summary: 上传项目文件
      description: 需 file:upload 权限。单文件上传，大小不超过 20MB。
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required: [file]
              properties:
                file:
                  type: string
                  format: binary
                  description: 待上传文件（单文件，≤20MB）
      responses:
        '201':
          description: 上传成功
          content:
            application/json:
              schema:
                type: object
                required: [file]
                properties:
                  file: { $ref: '#/components/schemas/FileMeta' }
        '400':
          description: 参数错误（bad_request，缺少文件）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '401':
          description: 未认证（unauthorized）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '403':
          description: 无权限（forbidden / api_key_forbidden / api_key_wrong_project）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
  /api/files/{id}:
    get:
      tags: [文件]
      summary: 下载文件
      description: 该项目成员或超管可下载；属资源版本的文件还需通过物料可见范围校验。仅接受用户 JWT，API 密钥调用返回 403 api_key_forbidden。响应带 Content-Disposition：attachment。
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
      responses:
        '200':
          description: 文件流（Content-Disposition：attachment）
          content:
            application/octet-stream:
              schema: { type: string, format: binary }
        '401':
          description: 未认证（unauthorized）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '403':
          description: 非项目成员或不可见（forbidden / api_key_forbidden）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '404':
          description: 文件不存在（not_found）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
  /api/projects/{id}/finance:
    get:
      tags: [财务]
      summary: 列出账目与汇总
      description: 任一项目成员可用。财务管理者（finance:manage 或 project:manage）见全部账目与完整汇总；finance:add 记账者仅见自己添加的账目且 summary 为 null。transactions 按创建时间倒序。
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
      responses:
        '200':
          description: 账目列表与汇总
          content:
            application/json:
              schema:
                type: object
                required: [transactions, summary]
                properties:
                  transactions:
                    type: array
                    items: { $ref: '#/components/schemas/TransactionItem' }
                  summary:
                    $ref: '#/components/schemas/FinanceSummary'
                    nullable: true
        '401':
          description: 未认证（unauthorized）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '403':
          description: 非项目成员（forbidden / api_key_forbidden / api_key_wrong_project）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
    post:
      tags: [财务]
      summary: 新建账目
      description: 需 finance:manage 或 finance:add 权限。金额以元提交（最多两位小数），服务端转换为整数分。payerUserId 与 splitAmong 必须是项目成员。
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [type, amount, payerUserId]
              properties:
                type: { type: string, enum: [income, expense] }
                amount:
                  type: number
                  description: 金额（元，必须为正，最多两位小数；也接受数字字符串）
                note: { type: string }
                payerUserId:
                  type: string
                  description: 支出为付款人，收入为收款人；必须是项目成员
                splitAmong:
                  type: array
                  items: { type: string }
                  description: 参与平摊人 userId 列表；缺省或空数组表示全体成员
          multipart/form-data:
            schema:
              type: object
              required: [type, amount, payerUserId]
              properties:
                type: { type: string, enum: [income, expense] }
                amount: { type: string, description: 金额（元，必须为正，最多两位小数） }
                note: { type: string }
                payerUserId: { type: string }
                splitAmong:
                  type: string
                  description: 参与平摊人 userId，JSON 数组字符串或逗号分隔
                files:
                  type: array
                  items: { type: string, format: binary }
                  description: 凭证附件，最多 10 个，单个 ≤20MB
      responses:
        '201':
          description: 创建成功
          content:
            application/json:
              schema:
                type: object
                required: [transaction]
                properties:
                  transaction: { $ref: '#/components/schemas/TransactionItem' }
        '400':
          description: 参数错误（bad_request，金额无效或成员校验失败）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '401':
          description: 未认证（unauthorized）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '403':
          description: 无记账权限（forbidden / api_key_forbidden / api_key_wrong_project）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
  /api/projects/{id}/finance/ticket:
    patch:
      tags: [财务]
      summary: 设置门票
      description: 需 finance:manage 权限。支持两种请求体——多票种（ticketTypes 数组，0~20 个，保存时清零旧单票字段）与旧单票格式（ticketPrice + ticketCount，兼容保留）；响应形态随请求体而定。
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                ticketTypes:
                  type: array
                  maxItems: 20
                  items:
                    type: object
                    required: [name, price, count]
                    properties:
                      name: { type: string, maxLength: 20 }
                      price: { type: number, description: 单价（元，≥0，最多两位小数） }
                      count: { type: integer, minimum: 0 }
                ticketPrice:
                  type: number
                  description: 旧单票格式——票价（元，≥0，最多两位小数；也接受数字字符串）
                ticketCount: { type: integer, minimum: 0, description: 旧单票格式——售票数量 }
      responses:
        '200':
          description: 设置成功。多票种请求返回 { ticketTypes, ticketIncomeCents }（金额单位为分）；旧单票请求返回 { ticketPriceCents, ticketCount }
          content:
            application/json:
              schema:
                type: object
                properties:
                  ticketTypes:
                    type: array
                    items:
                      type: object
                      required: [name, priceCents, count]
                      properties:
                        name: { type: string }
                        priceCents: { type: integer }
                        count: { type: integer }
                  ticketIncomeCents: { type: integer, description: 多票种形态——门票总收入（分） }
                  ticketPriceCents: { type: integer, description: 旧单票形态——票价（分） }
                  ticketCount: { type: integer, description: 旧单票形态——售票数量 }
        '400':
          description: 参数错误（bad_request，票种或票价无效）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '401':
          description: 未认证（unauthorized）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '403':
          description: 无财务管理权限（forbidden / api_key_forbidden / api_key_wrong_project）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
  /api/projects/{id}/finance/export:
    get:
      tags: [财务]
      summary: 导出账目 CSV
      description: 需 finance:manage 权限。导出目标成员相关账目（其为付款人、或 splitAmong 为空、或其在 splitAmong 中），按创建时间升序。响应为 text/csv; charset=utf-8，UTF-8 带 BOM，Content-Disposition：attachment；列为 日期,类型,金额(元),付款人,参与平摊,备注,添加人。
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
        - name: userId
          in: query
          schema: { type: string }
          description: 目标成员 userId，缺省为当前用户；必须是项目成员
      responses:
        '200':
          description: CSV 文件（text/csv; charset=utf-8，带 BOM）
          content:
            text/csv:
              schema: { type: string }
        '400':
          description: 参数错误（bad_request，userId 非项目成员）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '401':
          description: 未认证（unauthorized）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '403':
          description: 无财务管理权限（forbidden / api_key_forbidden / api_key_wrong_project）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
  /api/projects/{id}/finance/{txId}:
    patch:
      tags: [财务]
      summary: 编辑账目
      description: 需 finance:manage 或 finance:add 权限。财务管理者可改任意账目；finance:add 记账者仅能改自己添加的账目，改他人账目返回 403。不修改附件。
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
        - name: txId
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                type: { type: string, enum: [income, expense] }
                amount: { type: number, description: 金额（元，必须为正，最多两位小数） }
                note: { type: string }
                payerUserId: { type: string }
                splitAmong:
                  type: array
                  items: { type: string }
      responses:
        '200':
          description: 编辑成功
          content:
            application/json:
              schema:
                type: object
                required: [transaction]
                properties:
                  transaction: { $ref: '#/components/schemas/TransactionItem' }
        '400':
          description: 参数错误（bad_request）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '401':
          description: 未认证（unauthorized）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '403':
          description: 无权限或改动他人账目（forbidden）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '404':
          description: 账目不存在（not_found）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
    delete:
      tags: [财务]
      summary: 删除账目
      description: 需 finance:manage 或 finance:add 权限。财务管理者可删任意账目；finance:add 记账者仅能删自己添加的账目，删他人账目返回 403。
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
        - name: txId
          in: path
          required: true
          schema: { type: string }
      responses:
        '200':
          description: 删除成功
          content:
            application/json:
              schema:
                type: object
                required: [ok]
                properties:
                  ok: { type: boolean }
        '401':
          description: 未认证（unauthorized）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '403':
          description: 无权限或删除他人账目（forbidden）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '404':
          description: 账目不存在（not_found）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
  /api/projects/{id}/materials:
    get:
      tags: [物料管理]
      summary: 列出资源
      description: 项目成员可用，仅返回可见范围内的资源（可见范围优先于权限点）。
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
        - name: typeId
          in: query
          schema: { type: string }
          description: 按资源类型筛选
      responses:
        '200':
          description: 资源列表
          content:
            application/json:
              schema:
                type: object
                required: [resources]
                properties:
                  resources:
                    type: array
                    items: { $ref: '#/components/schemas/ResourceItem' }
        '401':
          description: 未认证（unauthorized）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '403':
          description: 非项目成员（forbidden / api_key_forbidden / api_key_wrong_project）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
    post:
      tags: [物料管理]
      summary: 创建资源
      description: 需 materials:manage 权限。支持 JSON 与 multipart 两种请求体；multipart 附带 file（可选，≤20MB，上传即创建版本 1）与 note（首版本备注），位图文件自动生成 WebP 缩略预览。
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [typeId, name]
              properties:
                typeId: { type: string }
                name: { type: string }
                description: { type: string }
                visibility: { $ref: '#/components/schemas/Visibility' }
          multipart/form-data:
            schema:
              type: object
              required: [typeId, name]
              properties:
                typeId: { type: string }
                name: { type: string }
                description: { type: string }
                visibility:
                  type: string
                  description: 可见范围，JSON 字符串（{ userIds, roleNames }）
                file:
                  type: string
                  format: binary
                  description: 可选，≤20MB，上传即创建版本 1
                note: { type: string, description: 首版本备注 }
      responses:
        '201':
          description: 创建成功
          content:
            application/json:
              schema:
                type: object
                required: [resource]
                properties:
                  resource: { $ref: '#/components/schemas/ResourceItem' }
        '400':
          description: 参数错误（bad_request，名称为空或类型不存在）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '401':
          description: 未认证（unauthorized）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '403':
          description: 无物料管理权限（forbidden / api_key_forbidden / api_key_wrong_project）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
  /api/projects/{id}/materials/types:
    get:
      tags: [物料管理]
      summary: 列出资源类型
      description: 项目成员可用，仅返回可见范围内的类型。
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
      responses:
        '200':
          description: 类型列表
          content:
            application/json:
              schema:
                type: object
                required: [types]
                properties:
                  types:
                    type: array
                    items: { $ref: '#/components/schemas/ResourceTypeItem' }
        '401':
          description: 未认证（unauthorized）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '403':
          description: 非项目成员（forbidden / api_key_forbidden / api_key_wrong_project）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
    post:
      tags: [物料管理]
      summary: 创建资源类型
      description: 需 materials:manage 权限。
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [name]
              properties:
                name: { type: string }
                visibility: { $ref: '#/components/schemas/Visibility' }
      responses:
        '201':
          description: 创建成功
          content:
            application/json:
              schema:
                type: object
                required: [type]
                properties:
                  type: { $ref: '#/components/schemas/ResourceTypeItem' }
        '400':
          description: 参数错误（bad_request，类型名称必填）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '401':
          description: 未认证（unauthorized）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '403':
          description: 无物料管理权限（forbidden / api_key_forbidden / api_key_wrong_project）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
  /api/projects/{id}/materials/types/{typeId}:
    patch:
      tags: [物料管理]
      summary: 编辑资源类型
      description: 需 materials:manage 权限。
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
        - name: typeId
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name: { type: string }
                visibility: { $ref: '#/components/schemas/Visibility' }
      responses:
        '200':
          description: 编辑成功
          content:
            application/json:
              schema:
                type: object
                required: [type]
                properties:
                  type: { $ref: '#/components/schemas/ResourceTypeItem' }
        '401':
          description: 未认证（unauthorized）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '403':
          description: 无物料管理权限（forbidden / api_key_forbidden / api_key_wrong_project）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '404':
          description: 类型不存在（not_found）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
    delete:
      tags: [物料管理]
      summary: 删除资源类型
      description: 需 materials:manage 权限。类型下仍有资源时返回 400。
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
        - name: typeId
          in: path
          required: true
          schema: { type: string }
      responses:
        '200':
          description: 删除成功
          content:
            application/json:
              schema:
                type: object
                required: [ok]
                properties:
                  ok: { type: boolean }
        '400':
          description: 类型下仍有资源（bad_request）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '401':
          description: 未认证（unauthorized）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '403':
          description: 无物料管理权限（forbidden / api_key_forbidden / api_key_wrong_project）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '404':
          description: 类型不存在（not_found）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
  /api/projects/{id}/materials/{resourceId}:
    get:
      tags: [物料管理]
      summary: 获取资源详情
      description: 项目成员可用；资源不可见（可见范围过滤）时返回 403。
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
        - name: resourceId
          in: path
          required: true
          schema: { type: string }
      responses:
        '200':
          description: 资源详情
          content:
            application/json:
              schema:
                type: object
                required: [resource]
                properties:
                  resource: { $ref: '#/components/schemas/ResourceItem' }
        '401':
          description: 未认证（unauthorized）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '403':
          description: 资源不可见（forbidden）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '404':
          description: 资源不存在（not_found）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
    patch:
      tags: [物料管理]
      summary: 编辑资源
      description: 需 materials:manage 权限。
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
        - name: resourceId
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name: { type: string }
                description: { type: string }
                typeId: { type: string }
                visibility: { $ref: '#/components/schemas/Visibility' }
      responses:
        '200':
          description: 编辑成功
          content:
            application/json:
              schema:
                type: object
                required: [resource]
                properties:
                  resource: { $ref: '#/components/schemas/ResourceItem' }
        '400':
          description: 参数错误（bad_request，typeId 类型不存在）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '401':
          description: 未认证（unauthorized）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '403':
          description: 无物料管理权限（forbidden / api_key_forbidden / api_key_wrong_project）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '404':
          description: 资源不存在（not_found）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
    delete:
      tags: [物料管理]
      summary: 删除资源
      description: 需 materials:manage 权限。删除资源及其全部版本与文件。
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
        - name: resourceId
          in: path
          required: true
          schema: { type: string }
      responses:
        '200':
          description: 删除成功
          content:
            application/json:
              schema:
                type: object
                required: [ok]
                properties:
                  ok: { type: boolean }
        '401':
          description: 未认证（unauthorized）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '403':
          description: 无物料管理权限（forbidden / api_key_forbidden / api_key_wrong_project）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '404':
          description: 资源不存在（not_found）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
  /api/projects/{id}/materials/{resourceId}/preview:
    get:
      tags: [物料管理]
      summary: 预览资源最新版本
      description: 项目成员且通过可见范围校验。取最新版本——有缩略预览时返回 image/webp；无预览但文件 mime 命中内联白名单（位图、PDF、Markdown、常见音视频）时返回原始文件流；否则 404。
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
        - name: resourceId
          in: path
          required: true
          schema: { type: string }
      responses:
        '200':
          description: 优先返回 WebP 缩略预览（image/webp）；无缩略预览但可内联时返回原始文件流（application/octet-stream，实际 Content-Type 为文件自身 mime）
          content:
            image/webp:
              schema: { type: string, format: binary }
            application/octet-stream:
              schema: { type: string, format: binary }
        '401':
          description: 未认证（unauthorized）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '403':
          description: 资源不可见（forbidden）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '404':
          description: 资源不存在、尚无版本或不可预览（not_found）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
  /api/projects/{id}/materials/{resourceId}/versions:
    get:
      tags: [物料管理]
      summary: 列出版本
      description: 项目成员且通过可见范围校验。按版本号倒序。
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
        - name: resourceId
          in: path
          required: true
          schema: { type: string }
      responses:
        '200':
          description: 版本列表
          content:
            application/json:
              schema:
                type: object
                required: [versions]
                properties:
                  versions:
                    type: array
                    items: { $ref: '#/components/schemas/ResourceVersionItem' }
        '401':
          description: 未认证（unauthorized）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '403':
          description: 资源不可见（forbidden）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '404':
          description: 资源不存在（not_found）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
    post:
      tags: [物料管理]
      summary: 上传新版本
      description: 需 materials:manage 权限。版本号自动递增并成为当前版；mime 命中位图白名单（png/jpeg/webp/gif）时自动生成 WebP 缩略预览（宽 ≤800px，体积 ≤100KB）。
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
        - name: resourceId
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required: [file]
              properties:
                file:
                  type: string
                  format: binary
                  description: 必填，≤20MB
                note: { type: string, description: 版本备注 }
      responses:
        '201':
          description: 创建成功
          content:
            application/json:
              schema:
                type: object
                required: [version]
                properties:
                  version: { $ref: '#/components/schemas/ResourceVersionItem' }
        '400':
          description: 参数错误（bad_request，缺少文件）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '401':
          description: 未认证（unauthorized）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '403':
          description: 无物料管理权限（forbidden / api_key_forbidden / api_key_wrong_project）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '404':
          description: 资源不存在（not_found）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
  /api/projects/{id}/materials/{resourceId}/versions/{version}/download:
    get:
      tags: [物料管理]
      summary: 下载指定版本原文件
      description: 项目成员且通过可见范围校验。响应带 Content-Disposition：attachment。
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
        - name: resourceId
          in: path
          required: true
          schema: { type: string }
        - name: version
          in: path
          required: true
          schema: { type: integer }
      responses:
        '200':
          description: 文件流（Content-Disposition：attachment）
          content:
            application/octet-stream:
              schema: { type: string, format: binary }
        '401':
          description: 未认证（unauthorized）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '403':
          description: 资源不可见（forbidden）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '404':
          description: 资源、版本或文件不存在（not_found）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
  /api/projects/{id}/materials/{resourceId}/versions/{version}/preview:
    get:
      tags: [物料管理]
      summary: 预览指定版本
      description: 项目成员且通过可见范围校验。有缩略预览时返回 image/webp；无预览但文件 mime 命中内联白名单（位图、PDF、Markdown、常见音视频）时返回原始文件流；否则 404。
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
        - name: resourceId
          in: path
          required: true
          schema: { type: string }
        - name: version
          in: path
          required: true
          schema: { type: integer }
      responses:
        '200':
          description: 优先返回 WebP 缩略预览（image/webp）；无缩略预览但可内联时返回原始文件流（application/octet-stream，实际 Content-Type 为文件自身 mime）
          content:
            image/webp:
              schema: { type: string, format: binary }
            application/octet-stream:
              schema: { type: string, format: binary }
        '401':
          description: 未认证（unauthorized）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '403':
          description: 资源不可见（forbidden）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '404':
          description: 资源或版本不存在、不可预览（not_found）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
  /api/projects/{id}/physical/categories:
    get:
      tags: [实物清单]
      summary: 列出实物分类
      description: 项目成员可用。按 order 升序返回；首次访问若项目无任何分类，自动填充默认 6 类（印刷品/设备器材/装饰布置/耗材文具/证件票券/其他）。
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
      responses:
        '200':
          description: 分类列表
          content:
            application/json:
              schema:
                type: object
                required: [categories]
                properties:
                  categories:
                    type: array
                    items: { $ref: '#/components/schemas/PhysicalCategoryItem' }
        '401':
          description: 未登录（unauthorized）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '403':
          description: 非项目成员或密钥越权（forbidden / api_key_wrong_project）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '404':
          description: 项目不存在（not_found）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
    post:
      tags: [实物清单]
      summary: 新建实物分类
      description: 需 materials:manage 权限（project:manage 等价放行，超管不受限）。
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [name]
              properties:
                name: { type: string }
      responses:
        '201':
          description: 创建成功
          content:
            application/json:
              schema:
                type: object
                required: [category]
                properties:
                  category: { $ref: '#/components/schemas/PhysicalCategoryItem' }
        '400':
          description: 参数错误（bad_request，分类名称不能为空）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '401':
          description: 未登录（unauthorized）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '403':
          description: 无权限或非项目成员（forbidden / api_key_wrong_project）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '404':
          description: 项目不存在（not_found）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
  /api/projects/{id}/physical/categories/reorder:
    patch:
      tags: [实物清单]
      summary: 重排实物分类
      description: 需 materials:manage 权限。按给定 id 数组顺序重排分类。
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [order]
              properties:
                order:
                  type: array
                  items: { type: string }
                  description: 分类 id 数组，按新顺序
      responses:
        '200':
          description: 重排后的分类列表
          content:
            application/json:
              schema:
                type: object
                required: [categories]
                properties:
                  categories:
                    type: array
                    items: { $ref: '#/components/schemas/PhysicalCategoryItem' }
        '400':
          description: 参数错误（bad_request，order 必须为分类 id 数组）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '401':
          description: 未登录（unauthorized）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '403':
          description: 无权限或非项目成员（forbidden / api_key_wrong_project）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '404':
          description: 项目不存在（not_found）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
  /api/projects/{id}/physical/categories/{catId}:
    patch:
      tags: [实物清单]
      summary: 更新实物分类
      description: 需 materials:manage 权限。
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
        - name: catId
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name: { type: string }
      responses:
        '200':
          description: 更新后的分类
          content:
            application/json:
              schema:
                type: object
                required: [category]
                properties:
                  category: { $ref: '#/components/schemas/PhysicalCategoryItem' }
        '401':
          description: 未登录（unauthorized）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '403':
          description: 无权限或非项目成员（forbidden / api_key_wrong_project）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '404':
          description: 项目或分类不存在（not_found）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
    delete:
      tags: [实物清单]
      summary: 删除实物分类
      description: 需 materials:manage 权限。分类下仍有物资时拒绝删除。
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
        - name: catId
          in: path
          required: true
          schema: { type: string }
      responses:
        '200':
          description: 删除成功
          content:
            application/json:
              schema:
                type: object
                required: [ok]
                properties:
                  ok: { type: boolean }
        '400':
          description: 该分类下仍有物资，无法删除（bad_request）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '401':
          description: 未登录（unauthorized）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '403':
          description: 无权限或非项目成员（forbidden / api_key_wrong_project）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '404':
          description: 项目或分类不存在（not_found）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
  /api/projects/{id}/physical/items:
    get:
      tags: [实物清单]
      summary: 列出物资条目
      description: 项目成员可用。支持按分类/状态/负责人/标签筛选与排序。
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
        - name: categoryId
          in: query
          schema: { type: string }
          description: 按分类 id 筛选
        - name: status
          in: query
          schema: { $ref: '#/components/schemas/PhysicalItemStatus' }
          description: 按状态筛选
        - name: responsibleId
          in: query
          schema: { type: string }
          description: 按负责人用户 id 筛选
        - name: tag
          in: query
          schema: { type: string }
          description: 按标签筛选
        - name: sort
          in: query
          schema:
            type: string
            enum: [name, status, plannedQty]
          description: 排序字段，默认 name
        - name: order
          in: query
          schema:
            type: string
            enum: [asc, desc]
          description: 排序方向，默认 asc
      responses:
        '200':
          description: 物资条目列表
          content:
            application/json:
              schema:
                type: object
                required: [items]
                properties:
                  items:
                    type: array
                    items: { $ref: '#/components/schemas/PhysicalItemItem' }
        '400':
          description: 筛选参数无效（bad_request）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '401':
          description: 未登录（unauthorized）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '403':
          description: 非项目成员或密钥越权（forbidden / api_key_wrong_project）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '404':
          description: 项目不存在（not_found）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
    post:
      tags: [实物清单]
      summary: 新建物资条目
      description: 需 materials:manage 权限。数量字段须为非负整数；responsibleId 须为项目成员。
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [name, categoryId]
              properties:
                name: { type: string }
                categoryId: { type: string }
                spec: { type: string }
                unit: { type: string }
                plannedQty: { type: integer }
                onHandQty: { type: integer }
                usedQty: { type: integer }
                lostQty: { type: integer }
                status: { $ref: '#/components/schemas/PhysicalItemStatus' }
                responsibleId: { type: string }
                location: { type: string }
                tags:
                  type: array
                  items: { type: string }
                note: { type: string }
      responses:
        '201':
          description: 创建成功
          content:
            application/json:
              schema:
                type: object
                required: [item]
                properties:
                  item: { $ref: '#/components/schemas/PhysicalItemItem' }
        '400':
          description: 参数错误（bad_request，名称/分类/数量/负责人校验失败）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '401':
          description: 未登录（unauthorized）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '403':
          description: 无权限或非项目成员（forbidden / api_key_wrong_project）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '404':
          description: 项目不存在（not_found）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
  /api/projects/{id}/physical/items/{itemId}:
    get:
      tags: [实物清单]
      summary: 获取物资条目
      description: 项目成员可用。跨项目访问返回 404。
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
        - name: itemId
          in: path
          required: true
          schema: { type: string }
      responses:
        '200':
          description: 物资条目详情
          content:
            application/json:
              schema:
                type: object
                required: [item]
                properties:
                  item: { $ref: '#/components/schemas/PhysicalItemItem' }
        '401':
          description: 未登录（unauthorized）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '403':
          description: 非项目成员或密钥越权（forbidden / api_key_wrong_project）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '404':
          description: 项目或物资不存在（not_found）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
    patch:
      tags: [实物清单]
      summary: 更新物资条目
      description: 需 materials:manage 权限。字段同新建（均可选）；responsibleId 传 null 或空字符串清除负责人。
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
        - name: itemId
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name: { type: string }
                categoryId: { type: string }
                spec: { type: string }
                unit: { type: string }
                plannedQty: { type: integer }
                onHandQty: { type: integer }
                usedQty: { type: integer }
                lostQty: { type: integer }
                status: { $ref: '#/components/schemas/PhysicalItemStatus' }
                responsibleId:
                  type: string
                  nullable: true
                  description: 传 null 或空字符串清除负责人
                location: { type: string }
                tags:
                  type: array
                  items: { type: string }
                note: { type: string }
      responses:
        '200':
          description: 更新后的物资条目
          content:
            application/json:
              schema:
                type: object
                required: [item]
                properties:
                  item: { $ref: '#/components/schemas/PhysicalItemItem' }
        '400':
          description: 参数错误（bad_request，数量/状态/负责人/分类校验失败）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '401':
          description: 未登录（unauthorized）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '403':
          description: 无权限或非项目成员（forbidden / api_key_wrong_project）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '404':
          description: 项目或物资不存在（not_found）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
    delete:
      tags: [实物清单]
      summary: 删除物资条目
      description: 需 materials:manage 权限。级联删除该物资的变动日志。
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
        - name: itemId
          in: path
          required: true
          schema: { type: string }
      responses:
        '200':
          description: 删除成功
          content:
            application/json:
              schema:
                type: object
                required: [ok]
                properties:
                  ok: { type: boolean }
        '401':
          description: 未登录（unauthorized）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '403':
          description: 无权限或非项目成员（forbidden / api_key_wrong_project）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '404':
          description: 项目或物资不存在（not_found）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
  /api/projects/{id}/physical/items/{itemId}/log:
    post:
      tags: [实物清单]
      summary: 记录物资数量变动
      description: 需 materials:manage 权限。数量类变动（adjust_on_hand/adjust_used/adjust_lost）的 delta 须为整数且结果不能为负；status_change 须带合法 status。每次成功记录一条日志（status_change 日志的 status 字段记录目标状态）。
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
        - name: itemId
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [type]
              properties:
                type:
                  type: string
                  enum: [adjust_on_hand, adjust_used, adjust_lost, status_change]
                delta:
                  type: integer
                  description: 数量类变动必填，可正可负，结果不能为负
                status: { $ref: '#/components/schemas/PhysicalItemStatus' }
                note: { type: string }
      responses:
        '200':
          description: 变动后的物资条目
          content:
            application/json:
              schema:
                type: object
                required: [item]
                properties:
                  item: { $ref: '#/components/schemas/PhysicalItemItem' }
        '400':
          description: 参数错误（bad_request，日志类型/变动量/状态校验失败或数量不能为负）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '401':
          description: 未登录（unauthorized）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '403':
          description: 无权限或非项目成员（forbidden / api_key_wrong_project）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '404':
          description: 项目或物资不存在（not_found）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
  /api/projects/{id}/physical/items/{itemId}/logs:
    get:
      tags: [实物清单]
      summary: 列出物资变动日志
      description: 项目成员可用。按时间倒序，最多 100 条。
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
        - name: itemId
          in: path
          required: true
          schema: { type: string }
      responses:
        '200':
          description: 变动日志列表
          content:
            application/json:
              schema:
                type: object
                required: [logs]
                properties:
                  logs:
                    type: array
                    items: { $ref: '#/components/schemas/PhysicalLogItem' }
        '401':
          description: 未登录（unauthorized）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '403':
          description: 非项目成员或密钥越权（forbidden / api_key_wrong_project）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '404':
          description: 项目或物资不存在（not_found）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
  /api/projects/{id}/physical/summary:
    get:
      tags: [实物清单]
      summary: 实物清单汇总
      description: 项目成员可用。返回总计与按分类聚合的计划/在库/使用/损耗/条目数。
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
      responses:
        '200':
          description: 汇总数据
          content:
            application/json:
              schema: { $ref: '#/components/schemas/PhysicalSummary' }
        '401':
          description: 未登录（unauthorized）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '403':
          description: 非项目成员或密钥越权（forbidden / api_key_wrong_project）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '404':
          description: 项目不存在（not_found）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
  /api/projects/{id}/accounts:
    get:
      tags: [平台账号]
      summary: 列出平台账号
      description: 项目成员可用，仅返回当前用户可见范围内（visibility 校验）的账号，不含密文/明文。超管不受可见范围限制。
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
        - name: platform
          in: query
          schema: { type: string }
          description: 按平台筛选（如 QQ/小红书/B站/微博/其他）
      responses:
        '200':
          description: 账号列表
          content:
            application/json:
              schema:
                type: object
                required: [accounts]
                properties:
                  accounts:
                    type: array
                    items:
                      type: object
                      required: [id, platform, account, mode, cipherKeySource, hasPassword, note, addedBy, visibility, createdAt]
                      properties:
                        id: { type: string }
                        platform: { type: string }
                        account: { type: string }
                        mode:
                          type: string
                          enum: [full, otp, contact]
                        cipherKeySource:
                          type: string
                          enum: [user, server]
                          nullable: true
                        hasPassword: { type: boolean }
                        note: { type: string }
                        addedBy:
                          type: object
                          nullable: true
                          required: [userId, name]
                          properties:
                            userId: { type: string }
                            name: { type: string }
                            contacts:
                              type: string
                              description: 仅 otp 模式返回
                        visibility: { $ref: '#/components/schemas/Visibility' }
                        createdAt: { type: string, format: date-time }
        '401':
          description: 未登录（unauthorized）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '403':
          description: 非项目成员或密钥越权（forbidden / api_key_wrong_project）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '404':
          description: 项目不存在（not_found）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
    post:
      tags: [平台账号]
      summary: 新增平台账号
      description: "需 accounts:manage 权限。mode 为 full 时密码二选一：浏览器加密（默认）传 passwordCipher（ANONv1 格式密文，服务端原样存储）；服务端加密传 cipherKeySource=server 加 password 明文（服务端加密后存储）。otp/contact 模式不传密码字段。"
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [platform, account, mode]
              properties:
                platform: { type: string }
                account: { type: string }
                mode:
                  type: string
                  enum: [full, otp, contact]
                note: { type: string }
                visibility: { $ref: '#/components/schemas/Visibility' }
                passwordCipher:
                  type: string
                  description: 浏览器加密密文（ANONv1 前缀，仅 full 模式）
                cipherKeySource:
                  type: string
                  enum: [server]
                  description: 传 server 表示由服务端加密（仅 full 模式）
                password:
                  type: string
                  description: 明文密码（仅 cipherKeySource=server 且 full 模式）
      responses:
        '201':
          description: 创建成功
          content:
            application/json:
              schema:
                type: object
                required: [account]
                properties:
                  account:
                    type: object
                    description: 结构同列表项
        '400':
          description: 参数错误（bad_request，平台/账号/mode/密码/visibility 校验失败）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '401':
          description: 未登录（unauthorized）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '403':
          description: 无权限或非项目成员（forbidden / api_key_wrong_project）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '404':
          description: 项目不存在（not_found）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
  /api/projects/{id}/accounts/{accountId}:
    patch:
      tags: [平台账号]
      summary: 更新平台账号
      description: 需 accounts:manage 权限且当前用户在该账号可见范围内。可更新 platform/account/mode/note/visibility；改密码传 password（server）或 passwordCipher（user，仅 full 模式）；mode 改为非 full 时清除密码。
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
        - name: accountId
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                platform: { type: string }
                account: { type: string }
                mode:
                  type: string
                  enum: [full, otp, contact]
                note: { type: string }
                visibility: { $ref: '#/components/schemas/Visibility' }
                passwordCipher:
                  type: string
                  description: 浏览器加密密文（ANONv1 前缀，仅 full 模式）
                cipherKeySource:
                  type: string
                  enum: [server]
                password:
                  type: string
                  description: 明文密码（仅 cipherKeySource=server 且 full 模式）
      responses:
        '200':
          description: 更新后的账号
          content:
            application/json:
              schema:
                type: object
                required: [account]
                properties:
                  account:
                    type: object
                    description: 结构同列表项
        '400':
          description: 参数错误（bad_request，字段或密码校验失败）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '401':
          description: 未登录（unauthorized）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '403':
          description: 无权限、非项目成员或不在可见范围内（forbidden / api_key_wrong_project）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '404':
          description: 项目或账号不存在（not_found）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
    delete:
      tags: [平台账号]
      summary: 删除平台账号
      description: 需 accounts:manage 权限且当前用户在该账号可见范围内。
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
        - name: accountId
          in: path
          required: true
          schema: { type: string }
      responses:
        '200':
          description: 删除成功
          content:
            application/json:
              schema:
                type: object
                required: [ok]
                properties:
                  ok: { type: boolean }
        '401':
          description: 未登录（unauthorized）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '403':
          description: 无权限、非项目成员或不在可见范围内（forbidden / api_key_wrong_project）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '404':
          description: 项目或账号不存在（not_found）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
  /api/projects/{id}/accounts/{accountId}/reveal:
    post:
      tags: [平台账号]
      summary: 查看账号密码
      description: 项目成员且在该账号可见范围内可用，仅 full 模式。server 模式返回 { password } 明文；user 模式返回 { cipher }（ANONv1 密文），前端提示输入保险库口令后用 WebCrypto 本地解密。非 full 模式或无密码返回 400。
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
        - name: accountId
          in: path
          required: true
          schema: { type: string }
      responses:
        '200':
          description: server 模式返回 password 明文；user 模式返回 cipher 密文
          content:
            application/json:
              schema:
                type: object
                properties:
                  password:
                    type: string
                    description: 明文密码（cipherKeySource=server 时）
                  cipher:
                    type: string
                    description: ANONv1 密文（cipherKeySource=user 时）
        '400':
          description: 该账号没有可查看的密码（bad_request，非 full 模式或无密码）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '401':
          description: 未登录（unauthorized）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '403':
          description: 非项目成员、密钥越权或不在可见范围内（forbidden / api_key_wrong_project）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '404':
          description: 项目或账号不存在（not_found）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
  /api/projects/{id}/work-modules:
    get:
      tags: [现场任务单]
      summary: 列出任务模块
      description: 列出项目全部现场任务模块，按创建时间升序。需项目成员身份。
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
      responses:
        '200':
          description: 模块列表
          content:
            application/json:
              schema:
                type: object
                required: [modules]
                properties:
                  modules:
                    type: array
                    items: { $ref: '#/components/schemas/WorkModuleItem' }
        '401':
          description: 未认证（unauthorized）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '403':
          description: 非项目成员或密钥不属于本项目（forbidden / api_key_wrong_project）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
    post:
      tags: [现场任务单]
      summary: 创建任务模块
      description: 需 work:manage 权限（project:manage 等价放行，超管不受限）。assigneeIds 必须全部为项目成员；创建后通知被分配成员。
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [name]
              properties:
                name: { type: string, maxLength: 100 }
                description: { type: string }
                location: { type: string }
                startAt: { type: string, format: date-time, description: 须为合法时间且不晚于 endAt }
                endAt: { type: string, format: date-time }
                requiredCount: { type: integer, minimum: 1, default: 1 }
                assigneeIds:
                  type: array
                  items: { type: string }
      responses:
        '201':
          description: 创建成功
          content:
            application/json:
              schema:
                type: object
                required: [module]
                properties:
                  module: { $ref: '#/components/schemas/WorkModuleItem' }
        '400':
          description: 参数错误（bad_request，名称缺失或超长、时间非法或先后颠倒、requiredCount 非 ≥1 整数、assigneeIds 含非法 ID 或非项目成员）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '401':
          description: 未认证（unauthorized）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '403':
          description: 无 work:manage 权限（forbidden / api_key_wrong_project）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
  /api/projects/{id}/work-modules/{mid}:
    patch:
      tags: [现场任务单]
      summary: 更新任务模块
      description: 需 work:manage 权限。字段均可选；startAt/endAt 需成对提交，任一键出现时两值同时重建，单传其一将把另一清空。替换 assigneeIds 时留任成员保留确认记录，被移除者清除。
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
        - name: mid
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name: { type: string, maxLength: 100 }
                description: { type: string }
                location: { type: string }
                startAt: { type: string, format: date-time, nullable: true }
                endAt: { type: string, format: date-time, nullable: true }
                requiredCount: { type: integer, minimum: 1 }
                assigneeIds:
                  type: array
                  items: { type: string }
      responses:
        '200':
          description: 更新成功
          content:
            application/json:
              schema:
                type: object
                required: [module]
                properties:
                  module: { $ref: '#/components/schemas/WorkModuleItem' }
        '400':
          description: 参数错误（bad_request）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '401':
          description: 未认证（unauthorized）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '403':
          description: 无 work:manage 权限（forbidden / api_key_wrong_project）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '404':
          description: 模块不存在（not_found，含跨项目 mid）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
    delete:
      tags: [现场任务单]
      summary: 删除任务模块
      description: 需 work:manage 权限。
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
        - name: mid
          in: path
          required: true
          schema: { type: string }
      responses:
        '200':
          description: 删除成功
          content:
            application/json:
              schema:
                type: object
                required: [ok]
                properties:
                  ok: { type: boolean }
        '401':
          description: 未认证（unauthorized）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '403':
          description: 无 work:manage 权限（forbidden / api_key_wrong_project）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '404':
          description: 模块不存在（not_found）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
  /api/projects/{id}/work-modules/{mid}/confirm:
    post:
      tags: [现场任务单]
      summary: 确认任务分配
      description: 确认成员与模块的分配关系，记录确认时间与确认人；重复确认幂等，不刷新时间戳。不传 userId 为确认本人（项目成员即可）；传 userId 为代他人确认，需 work:manage 或 project:manage。
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
        - name: mid
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                userId: { type: string, description: 目标成员；缺省为本人 }
      responses:
        '200':
          description: 确认成功
          content:
            application/json:
              schema:
                type: object
                required: [module]
                properties:
                  module: { $ref: '#/components/schemas/WorkModuleItem' }
        '400':
          description: 目标未被分配到该模块（bad_request）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '401':
          description: 未认证（unauthorized）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '403':
          description: 无权代他人确认（forbidden / api_key_wrong_project）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '404':
          description: 模块不存在（not_found）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
  /api/projects/{id}/work-modules/{mid}/unconfirm:
    post:
      tags: [现场任务单]
      summary: 取消任务确认
      description: 清除 confirmedAt/confirmedBy。请求体与权限同 confirm。
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
        - name: mid
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                userId: { type: string, description: 目标成员；缺省为本人 }
      responses:
        '200':
          description: 取消成功
          content:
            application/json:
              schema:
                type: object
                required: [module]
                properties:
                  module: { $ref: '#/components/schemas/WorkModuleItem' }
        '400':
          description: 目标未被分配到该模块（bad_request）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '401':
          description: 未认证（unauthorized）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '403':
          description: 无权代他人取消确认（forbidden / api_key_wrong_project）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '404':
          description: 模块不存在（not_found）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
  /api/projects/{id}/work-modules/{mid}/checkin:
    post:
      tags: [现场任务单]
      summary: 任务签到
      description: 记录 checkedInAt；重复签到幂等，不刷新时间戳。请求体与权限同 confirm（不传 userId 为本人签到，项目成员即可）。
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
        - name: mid
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                userId: { type: string, description: 目标成员；缺省为本人 }
      responses:
        '200':
          description: 签到成功
          content:
            application/json:
              schema:
                type: object
                required: [module]
                properties:
                  module: { $ref: '#/components/schemas/WorkModuleItem' }
        '400':
          description: 目标未被分配到该模块（bad_request）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '401':
          description: 未认证（unauthorized）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '403':
          description: 无权代他人签到（forbidden / api_key_wrong_project）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '404':
          description: 模块不存在（not_found）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
  /api/projects/{id}/work-modules/{mid}/finish:
    post:
      tags: [现场任务单]
      summary: 标记任务完成
      description: 记录 completedAt；未签到的成员同时补签到；重复完成幂等，不刷新时间戳。请求体与权限同 confirm。
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
        - name: mid
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                userId: { type: string, description: 目标成员；缺省为本人 }
      responses:
        '200':
          description: 标记成功
          content:
            application/json:
              schema:
                type: object
                required: [module]
                properties:
                  module: { $ref: '#/components/schemas/WorkModuleItem' }
        '400':
          description: 目标未被分配到该模块（bad_request）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '401':
          description: 未认证（unauthorized）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '403':
          description: 无权代他人标记完成（forbidden / api_key_wrong_project）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '404':
          description: 模块不存在（not_found）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
  /api/projects/{id}/work-sheet:
    get:
      tags: [现场任务单]
      summary: 查看本人任务单
      description: 本人的现场任务单（项目成员）。items 按 startAt、createdAt 升序；无分配时为空数组。
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
      responses:
        '200':
          description: 本人任务单
          content:
            application/json:
              schema: { $ref: '#/components/schemas/WorkSheetData' }
        '401':
          description: 未认证（unauthorized）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '403':
          description: 非项目成员（forbidden / api_key_wrong_project）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
  /api/projects/{id}/work-sheet/{userId}:
    get:
      tags: [现场任务单]
      summary: 查看指定成员任务单
      description: 需 work:manage 权限（project:manage 等价放行，超管不受限）。
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
        - name: userId
          in: path
          required: true
          schema: { type: string }
      responses:
        '200':
          description: 指定成员任务单
          content:
            application/json:
              schema: { $ref: '#/components/schemas/WorkSheetData' }
        '401':
          description: 未认证（unauthorized）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '403':
          description: 无 work:manage 权限（forbidden / api_key_wrong_project）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '404':
          description: 该用户不是项目成员（not_found）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
  /api/projects/{id}/onsite:
    get:
      tags: [现场任务单]
      summary: 现场模式聚合
      description: 现场模式聚合接口（项目成员）。myModules 为指派给本人的模块（含本人确认/签到/完成状态与 state 流转）；emergency 为可见的紧急/重要公告（≤5 条）；contacts 为填写了联系方式的成员；incidents 按权限可见（work:manage 见全部，普通成员仅自己上报的）；rundowns 为舞台执行聚合（≤5 条，执行中的 Rundown 恒在列，未开始的仅取开始时间 ±24h 窗口内）；myPermissions 为本人权限点数组（前端据此显隐现场页权限入口）。
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
      responses:
        '200':
          description: 现场模式聚合数据
          content:
            application/json:
              schema:
                type: object
                required: [now, myModules, emergency, contacts, incidents, rundowns, myPermissions]
                properties:
                  now: { type: string, format: date-time, description: 服务器当前时间 }
                  myModules:
                    type: array
                    description: 指派给本人的模块，按 state（current/upcoming/done）再按开始时间排序
                    items:
                      type: object
                      required: [id, name, location, startAt, endAt, myAssignee, state]
                      properties:
                        id: { type: string }
                        name: { type: string }
                        location: { type: string, nullable: true }
                        startAt: { type: string, format: date-time, nullable: true }
                        endAt: { type: string, format: date-time, nullable: true }
                        myAssignee:
                          type: object
                          nullable: true
                          description: 本人在该模块的确认/签到/完成时间
                          required: [confirmedAt, checkedInAt, completedAt]
                          properties:
                            confirmedAt: { type: string, format: date-time, nullable: true }
                            checkedInAt: { type: string, format: date-time, nullable: true }
                            completedAt: { type: string, format: date-time, nullable: true }
                        state: { type: string, enum: [current, upcoming, done], description: current 进行中；upcoming 未开始；done 已完成 }
                  emergency:
                    type: array
                    description: 可见的紧急/重要公告（≤5 条）
                    items:
                      type: object
                      required: [id, title, content, type, publishedAt]
                      properties:
                        id: { type: string }
                        title: { type: string }
                        content: { type: string }
                        type: { type: string, enum: [emergency, important] }
                        publishedAt: { type: string, format: date-time }
                  contacts:
                    type: array
                    description: 填写了联系方式的成员
                    items:
                      type: object
                      required: [userId, name, roleName, contacts]
                      properties:
                        userId: { type: string }
                        name: { type: string }
                        roleName: { type: string, nullable: true }
                        contacts:
                          type: array
                          items:
                            type: object
                            required: [platform, value]
                            properties:
                              platform: { type: string }
                              value: { type: string }
                  incidents:
                    type: array
                    description: 按权限可见的现场异常（work:manage 见全部，普通成员仅自己上报的）
                    items: { $ref: '#/components/schemas/OnsiteIncident' }
                  rundowns:
                    type: array
                    description: 舞台执行聚合（≤5 条；执行中恒在列，未开始的取开始时间 ±24h 窗口内）
                    items:
                      type: object
                      required: [id, name, status, startAt, itemCount, currentIndex, currentItemId, currentItemName, currentPlannedStart, currentActualStart, shiftMin]
                      properties:
                        id: { type: string }
                        name: { type: string }
                        status: { type: string, enum: [idle, running] }
                        startAt: { type: string, format: date-time }
                        itemCount: { type: integer }
                        currentIndex: { type: integer, nullable: true, description: idle 时为 null }
                        currentItemId: { type: string, nullable: true }
                        currentItemName: { type: string, nullable: true }
                        currentPlannedStart: { type: string, format: date-time, nullable: true }
                        currentActualStart: { type: string, format: date-time, nullable: true }
                        shiftMin: { type: integer, description: 整体偏移分钟数；idle 时为 0 }
                  myPermissions:
                    type: array
                    description: 本人权限点数组
                    items: { type: string }
        '401':
          description: 未认证（unauthorized）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '403':
          description: 非项目成员（forbidden / api_key_wrong_project）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
  /api/projects/{id}/onsite/incidents:
    get:
      tags: [现场任务单]
      summary: 列出现场异常
      description: work:manage/project:manage 见全部，普通成员仅见自己上报的；按时间倒序，最多 50 条。
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
      responses:
        '200':
          description: 异常列表
          content:
            application/json:
              schema:
                type: object
                required: [incidents]
                properties:
                  incidents:
                    type: array
                    items: { $ref: '#/components/schemas/OnsiteIncident' }
        '401':
          description: 未认证（unauthorized）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '403':
          description: 非项目成员（forbidden / api_key_wrong_project）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
    post:
      tags: [现场任务单]
      summary: 上报现场异常
      description: 项目成员上报异常并通知项目管理者。moduleId 须为本项目任务模块。
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [category, note]
              properties:
                category:
                  type: string
                  enum: [equipment, staff, material, venue, safety, other]
                note: { type: string, maxLength: 500 }
                moduleId: { type: string, description: 关联任务模块，可选 }
      responses:
        '201':
          description: 上报成功
          content:
            application/json:
              schema:
                type: object
                required: [incident]
                properties:
                  incident: { $ref: '#/components/schemas/OnsiteIncident' }
        '400':
          description: 参数错误（bad_request，category 非法、备注为空或超长、模块不存在）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '401':
          description: 未认证（unauthorized）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '403':
          description: 非项目成员（forbidden / api_key_wrong_project）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
  /api/projects/{id}/onsite/incidents/{iid}/resolve:
    post:
      tags: [现场任务单]
      summary: 标记异常已处理
      description: 需 work:manage 权限。记录处理人与时间；已处理时幂等不刷新。
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
        - name: iid
          in: path
          required: true
          schema: { type: string }
      responses:
        '200':
          description: 处理成功
          content:
            application/json:
              schema:
                type: object
                required: [incident]
                properties:
                  incident: { $ref: '#/components/schemas/OnsiteIncident' }
        '401':
          description: 未认证（unauthorized）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '403':
          description: 无 work:manage 权限（forbidden / api_key_wrong_project）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '404':
          description: 异常记录不存在（not_found）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
  /api/projects/{id}/stage-rundowns:
    get:
      tags: [舞台 Rundown]
      summary: 列出 Rundown
      description: 项目成员可查看。按 startAt、createdAt 升序；totalDurationMin 为全部节目时长之和。每项目可建多份 Rundown（如 Day1/Day2、主/副舞台）。
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
      responses:
        '200':
          description: Rundown 摘要列表
          content:
            application/json:
              schema:
                type: object
                required: [rundowns]
                properties:
                  rundowns:
                    type: array
                    items: { $ref: '#/components/schemas/StageRundownSummary' }
        '401':
          description: 未认证（unauthorized）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '403':
          description: 无权限（forbidden / api_key_forbidden / api_key_wrong_project）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
    post:
      tags: [舞台 Rundown]
      summary: 创建 Rundown
      description: 需 tools:manage 权限（project:manage 等价放行，超管不受限）。name trim 后非空；startAt 须为可解析时间；创建后 items 为空数组。
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [name, startAt]
              properties:
                name: { type: string }
                startAt: { type: string, format: date-time, description: 计划开始时刻 }
                note: { type: string }
      responses:
        '201':
          description: 创建成功
          content:
            application/json:
              schema:
                type: object
                required: [rundown]
                properties:
                  rundown: { $ref: '#/components/schemas/StageRundown' }
        '400':
          description: 参数错误（bad_request：名称为空或 startAt 不可解析）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '401':
          description: 未认证（unauthorized）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '403':
          description: 无权限（forbidden / api_key_forbidden / api_key_wrong_project）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
  /api/projects/{id}/stage-rundowns/{rid}:
    get:
      tags: [舞台 Rundown]
      summary: 获取 Rundown 详情
      description: 项目成员可查看。附件已解析为 StageRundownAttachment；缺失的文件 id 跳过。跨项目 rid 按 404 处理。
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
        - name: rid
          in: path
          required: true
          schema: { type: string }
      responses:
        '200':
          description: Rundown 详情
          content:
            application/json:
              schema:
                type: object
                required: [rundown]
                properties:
                  rundown: { $ref: '#/components/schemas/StageRundown' }
        '401':
          description: 未认证（unauthorized）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '403':
          description: 无权限（forbidden / api_key_forbidden / api_key_wrong_project）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '404':
          description: Rundown 不存在（not_found）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
    patch:
      tags: [舞台 Rundown]
      summary: 更新 Rundown
      description: 需 tools:manage 权限。请求字段同创建（均可选）；name 提供但 trim 后为空、startAt 提供但不可解析返回 400。执行中（execution.status 为 running）仅 name/note 可改，修改 startAt 返回 409。
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
        - name: rid
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name: { type: string }
                startAt: { type: string, format: date-time }
                note: { type: string }
      responses:
        '200':
          description: 更新后的 Rundown
          content:
            application/json:
              schema:
                type: object
                required: [rundown]
                properties:
                  rundown: { $ref: '#/components/schemas/StageRundown' }
        '400':
          description: 参数错误（bad_request）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '401':
          description: 未认证（unauthorized）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '403':
          description: 无权限（forbidden / api_key_forbidden / api_key_wrong_project）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '404':
          description: Rundown 不存在（not_found）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '409':
          description: 执行中禁止修改 startAt（EXECUTION_RUNNING）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
    delete:
      tags: [舞台 Rundown]
      summary: 删除 Rundown
      description: 需 tools:manage 权限。级联删除全部节目的素材文件（存储对象与 File 文档）与现场大屏分享文档。执行中禁止删除（409）。
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
        - name: rid
          in: path
          required: true
          schema: { type: string }
      responses:
        '200':
          description: 删除成功
          content:
            application/json:
              schema:
                type: object
                required: [ok]
                properties:
                  ok: { type: boolean }
        '401':
          description: 未认证（unauthorized）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '403':
          description: 无权限（forbidden / api_key_forbidden / api_key_wrong_project）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '404':
          description: Rundown 不存在（not_found）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '409':
          description: 执行中禁止删除（EXECUTION_RUNNING）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
  /api/projects/{id}/stage-rundowns/{rid}/items:
    post:
      tags: [舞台 Rundown]
      summary: 新增节目
      description: 需 tools:manage 权限。multipart 表单；上传文件按顺序落库为 File 文档并记为节目附件。执行中禁止编排变更（409）。
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
        - name: rid
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required: [name, durationMin]
              properties:
                name: { type: string, description: 节目名，trim 后非空 }
                durationMin: { type: integer, description: 时长（分钟），须为 1–1440 的整数 }
                participants:
                  type: string
                  description: 参与人员的 JSON 字符串数组（元素为含 cn、contact 字段的对象）；解析失败或非数组返回 400；仅保留 cn trim 非空项，contact 缺省为空串
                note: { type: string }
                files:
                  type: array
                  description: 节目素材文件，最多 10 个，单个 ≤ 20MB
                  items: { type: string, format: binary }
      responses:
        '201':
          description: 新增成功
          content:
            application/json:
              schema:
                type: object
                required: [item]
                properties:
                  item: { $ref: '#/components/schemas/StageRundownItem' }
        '400':
          description: 参数错误（bad_request：名称为空、durationMin 非 1–1440 整数或 participants 格式无效）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '401':
          description: 未认证（unauthorized）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '403':
          description: 无权限（forbidden / api_key_forbidden / api_key_wrong_project）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '404':
          description: Rundown 不存在（not_found）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '409':
          description: 执行中禁止编排变更（EXECUTION_RUNNING）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
  /api/projects/{id}/stage-rundowns/{rid}/items/reorder:
    patch:
      tags: [舞台 Rundown]
      summary: 重排节目顺序
      description: 需 tools:manage 权限。order 必须与现有节目一一对应（长度相等、id 集合完全相同），否则 400。执行中仅可在未执行节目槽位内重排：当前节目或已有实际记录的节目所在位置必须原样，否则 409「执行中仅可调整未执行节目的顺序」。
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
        - name: rid
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [order]
              properties:
                order:
                  type: array
                  description: 节目 id 的全量排序
                  items: { type: string }
      responses:
        '200':
          description: 重排后的 Rundown
          content:
            application/json:
              schema:
                type: object
                required: [rundown]
                properties:
                  rundown: { $ref: '#/components/schemas/StageRundown' }
        '400':
          description: 参数错误（bad_request：order 未与现有节目一一对应）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '401':
          description: 未认证（unauthorized）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '403':
          description: 无权限（forbidden / api_key_forbidden / api_key_wrong_project）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '404':
          description: Rundown 不存在（not_found）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '409':
          description: 执行中仅可调整未执行节目的顺序（EXECUTION_RUNNING）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
  /api/projects/{id}/stage-rundowns/{rid}/items/{itemId}:
    patch:
      tags: [舞台 Rundown]
      summary: 更新节目
      description: 需 tools:manage 权限。multipart 表单，字段同新增（均可选）。removeAttachmentIds 命中的附件从节目移除并级联删除存储对象与 File 文档；新上传文件追加在附件列表末尾。执行中禁止编排变更（409）。
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
        - name: rid
          in: path
          required: true
          schema: { type: string }
        - name: itemId
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              properties:
                name: { type: string, description: 节目名，提供但 trim 后为空返回 400 }
                durationMin: { type: integer, description: 时长（分钟），须为 1–1440 的整数 }
                participants:
                  type: string
                  description: 参与人员的 JSON 字符串数组（元素为含 cn、contact 字段的对象）
                note: { type: string }
                removeAttachmentIds:
                  type: string
                  description: 待移除附件 id 的 JSON 字符串数组
                files:
                  type: array
                  description: 新追加的素材文件，最多 10 个，单个 ≤ 20MB
                  items: { type: string, format: binary }
      responses:
        '200':
          description: 更新后的节目
          content:
            application/json:
              schema:
                type: object
                required: [item]
                properties:
                  item: { $ref: '#/components/schemas/StageRundownItem' }
        '400':
          description: 参数错误（bad_request）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '401':
          description: 未认证（unauthorized）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '403':
          description: 无权限（forbidden / api_key_forbidden / api_key_wrong_project）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '404':
          description: Rundown 或节目不存在（not_found）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '409':
          description: 执行中禁止编排变更（EXECUTION_RUNNING）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
    delete:
      tags: [舞台 Rundown]
      summary: 删除节目
      description: 需 tools:manage 权限。删除节目并级联清理其附件文件。执行中禁止编排变更（409）。
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
        - name: rid
          in: path
          required: true
          schema: { type: string }
        - name: itemId
          in: path
          required: true
          schema: { type: string }
      responses:
        '200':
          description: 删除成功
          content:
            application/json:
              schema:
                type: object
                required: [ok]
                properties:
                  ok: { type: boolean }
        '401':
          description: 未认证（unauthorized）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '403':
          description: 无权限（forbidden / api_key_forbidden / api_key_wrong_project）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '404':
          description: Rundown 或节目不存在（not_found）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '409':
          description: 执行中禁止编排变更（EXECUTION_RUNNING）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
  /api/projects/{id}/stage-rundowns/{rid}/execution/start:
    post:
      tags: [舞台 Rundown]
      summary: 开始执行
      description: 需 tools:manage 权限。缺省从首节目开始；空节目单返回 400。重置执行记录后从目标节目起计（status 为 running、shiftMin 归零、actuals 仅含目标节目）；finished 状态调用等于重新执行（清旧记录）。
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
        - name: rid
          in: path
          required: true
          schema: { type: string }
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                itemId: { type: string, description: 起始节目 id，缺省从首节目开始；提供但不存在返回 400 }
      responses:
        '200':
          description: 执行中的 Rundown
          content:
            application/json:
              schema:
                type: object
                required: [rundown]
                properties:
                  rundown: { $ref: '#/components/schemas/StageRundown' }
        '400':
          description: 参数错误（bad_request：空节目单或节目不存在）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '401':
          description: 未认证（unauthorized）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '403':
          description: 无权限（forbidden / api_key_forbidden / api_key_wrong_project）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '404':
          description: Rundown 不存在（not_found）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '409':
          description: 已在执行中（ALREADY_RUNNING）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
  /api/projects/{id}/stage-rundowns/{rid}/execution/advance:
    post:
      tags: [舞台 Rundown]
      summary: 推进到下一节目
      description: 需 tools:manage 权限。当前节目记 endedAt 并推进到下一节目；已是末节目则转为 finished（finishedAt 记录、currentItemId 置空）。
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
        - name: rid
          in: path
          required: true
          schema: { type: string }
      responses:
        '200':
          description: 更新后的 Rundown
          content:
            application/json:
              schema:
                type: object
                required: [rundown]
                properties:
                  rundown: { $ref: '#/components/schemas/StageRundown' }
        '401':
          description: 未认证（unauthorized）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '403':
          description: 无权限（forbidden / api_key_forbidden / api_key_wrong_project）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '404':
          description: Rundown 不存在（not_found）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '409':
          description: 当前不在执行中（NOT_RUNNING）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
  /api/projects/{id}/stage-rundowns/{rid}/execution/jump:
    post:
      tags: [舞台 Rundown]
      summary: 跳转到指定节目
      description: 需 tools:manage 权限。当前节目记 endedAt，目标节目旧 actual 移除后重新起计；跳转目标为当前节目时幂等返回（不写库）。
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
        - name: rid
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [itemId]
              properties:
                itemId: { type: string, description: 目标节目 id；缺失或不存在返回 400 }
      responses:
        '200':
          description: 更新后的 Rundown
          content:
            application/json:
              schema:
                type: object
                required: [rundown]
                properties:
                  rundown: { $ref: '#/components/schemas/StageRundown' }
        '400':
          description: 参数错误（bad_request：缺少节目或节目不存在）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '401':
          description: 未认证（unauthorized）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '403':
          description: 无权限（forbidden / api_key_forbidden / api_key_wrong_project）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '404':
          description: Rundown 不存在（not_found）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '409':
          description: 当前不在执行中（NOT_RUNNING）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
  /api/projects/{id}/stage-rundowns/{rid}/execution/shift:
    post:
      tags: [舞台 Rundown]
      summary: 累加顺延分钟
      description: 需 tools:manage 权限。minutes 须为 ±240 内整数，累加后 |shiftMin| 超过 1440 返回 400；「清零顺延」由前端发送当前 shiftMin 的相反数。未开始节目预计时间 = 计划 + shiftMin。
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
        - name: rid
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [minutes]
              properties:
                minutes: { type: integer, description: 顺延分钟数（可负表示提前），单次 ±240 内 }
      responses:
        '200':
          description: 更新后的 Rundown
          content:
            application/json:
              schema:
                type: object
                required: [rundown]
                properties:
                  rundown: { $ref: '#/components/schemas/StageRundown' }
        '400':
          description: 参数错误（bad_request：minutes 非 ±240 内整数或累计超出 ±1440）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '401':
          description: 未认证（unauthorized）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '403':
          description: 无权限（forbidden / api_key_forbidden / api_key_wrong_project）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '404':
          description: Rundown 不存在（not_found）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '409':
          description: 当前不在执行中（NOT_RUNNING）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
  /api/projects/{id}/stage-rundowns/{rid}/execution/finish:
    post:
      tags: [舞台 Rundown]
      summary: 结束执行
      description: 需 tools:manage 权限。当前节目未结束的 actual 记 endedAt，状态转为 finished。
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
        - name: rid
          in: path
          required: true
          schema: { type: string }
      responses:
        '200':
          description: 更新后的 Rundown
          content:
            application/json:
              schema:
                type: object
                required: [rundown]
                properties:
                  rundown: { $ref: '#/components/schemas/StageRundown' }
        '401':
          description: 未认证（unauthorized）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '403':
          description: 无权限（forbidden / api_key_forbidden / api_key_wrong_project）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '404':
          description: Rundown 不存在（not_found）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '409':
          description: 当前不在执行中（NOT_RUNNING）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
  /api/projects/{id}/stage-rundowns/{rid}/execution/reset:
    post:
      tags: [舞台 Rundown]
      summary: 重置执行状态
      description: 需 tools:manage 权限。任何状态回到 idle 初始态（清空 actuals 与 shiftMin）；idle 时幂等。
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
        - name: rid
          in: path
          required: true
          schema: { type: string }
      responses:
        '200':
          description: 更新后的 Rundown
          content:
            application/json:
              schema:
                type: object
                required: [rundown]
                properties:
                  rundown: { $ref: '#/components/schemas/StageRundown' }
        '401':
          description: 未认证（unauthorized）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '403':
          description: 无权限（forbidden / api_key_forbidden / api_key_wrong_project）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '404':
          description: Rundown 不存在（not_found）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
  /api/projects/{id}/stage-rundowns/{rid}/screen-share:
    get:
      tags: [舞台 Rundown]
      summary: 获取大屏分享信息
      description: 需 tools:manage 权限。惰性创建分享文档后返回（初始 enabled 为 false）。
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
        - name: rid
          in: path
          required: true
          schema: { type: string }
      responses:
        '200':
          description: 分享信息
          content:
            application/json:
              schema:
                type: object
                required: [share]
                properties:
                  share: { $ref: '#/components/schemas/StageScreenShareInfo' }
        '401':
          description: 未认证（unauthorized）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '403':
          description: 无权限（forbidden / api_key_forbidden / api_key_wrong_project）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '404':
          description: Rundown 不存在（not_found）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
    put:
      tags: [舞台 Rundown]
      summary: 更新大屏分享设置
      description: 需 tools:manage 权限。regenerate 为 true 时换新 token，旧链接立即失效。
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
        - name: rid
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                enabled: { type: boolean, description: 是否开启公开访问 }
                regenerate: { type: boolean, description: 为 true 时重新生成 token }
      responses:
        '200':
          description: 更新后的分享信息
          content:
            application/json:
              schema:
                type: object
                required: [share]
                properties:
                  share: { $ref: '#/components/schemas/StageScreenShareInfo' }
        '401':
          description: 未认证（unauthorized）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '403':
          description: 无权限（forbidden / api_key_forbidden / api_key_wrong_project）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '404':
          description: Rundown 不存在（not_found）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
  /api/public/rundown-screen/{token}:
    get:
      tags: [舞台 Rundown]
      summary: 获取现场大屏公开数据
      security: []
      description: 免登录公开端点，限流 300 次/分钟/IP。分享不存在、未开启或 Rundown 已删除一律 404「链接不存在或已关闭」。白名单输出：items 不含 note 与 attachments，participants 仅 cn（联系方式不外泄）；announcements 仅紧急类型、未过期、全员可见（visibility 双空）的前 5 条，按 isPinned、publishedAt 倒序。
      parameters:
        - name: token
          in: path
          required: true
          schema: { type: string }
      responses:
        '200':
          description: 大屏白名单数据
          content:
            application/json:
              schema:
                type: object
                required: [projectName, now, rundown, announcements]
                properties:
                  projectName: { type: string }
                  now: { type: string, format: date-time, description: 服务器当前时刻 }
                  rundown:
                    type: object
                    required: [name, startAt, items, execution]
                    properties:
                      name: { type: string }
                      startAt: { type: string, format: date-time }
                      items:
                        type: array
                        items:
                          type: object
                          required: [id, name, durationMin, participants]
                          properties:
                            id: { type: string }
                            name: { type: string }
                            durationMin: { type: integer }
                            participants:
                              type: array
                              items:
                                type: object
                                required: [cn]
                                properties:
                                  cn: { type: string }
                      execution: { $ref: '#/components/schemas/StageExecution' }
                  announcements:
                    type: array
                    items:
                      type: object
                      required: [id, title, content, publishedAt]
                      properties:
                        id: { type: string }
                        title: { type: string }
                        content: { type: string }
                        publishedAt: { type: string, format: date-time }
        '404':
          description: 链接不存在或已关闭（not_found）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '429':
          description: 请求过于频繁（rate_limited）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
  /api/projects/{id}/stage-signups:
    get:
      summary: 列出报名批次
      description: 项目成员可查看，按 startAt、createdAt 升序。
      tags: [舞台报名审核]
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
      responses:
        '200':
          description: 报名批次摘要列表
          content:
            application/json:
              schema:
                type: object
                required: [signups]
                properties:
                  signups:
                    type: array
                    items: { $ref: '#/components/schemas/StageSignupSummary' }
        '401':
          description: 未认证（unauthorized）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
    post:
      summary: 创建报名批次
      description: 需 tools:manage 权限（project:manage 等价放行，超管不受限）。name trim 后非空；startAt/endAt 须为可解析时间；endAt 必须晚于 startAt。
      tags: [舞台报名审核]
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [name, startAt, endAt]
              properties:
                name: { type: string, description: 批次名称 }
                startAt: { type: string, format: date-time, description: 报名开始时间 }
                endAt: { type: string, format: date-time, description: 报名结束时间，必须晚于 startAt }
                note: { type: string, description: 备注 }
      responses:
        '201':
          description: 创建成功，items 为空数组
          content:
            application/json:
              schema:
                type: object
                required: [signup]
                properties:
                  signup: { $ref: '#/components/schemas/StageSignup' }
        '400':
          description: 参数错误（bad_request）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '401':
          description: 未认证（unauthorized）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '403':
          description: 无权限（forbidden / api_key_forbidden / api_key_wrong_project）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
  /api/projects/{id}/stage-signups/{sid}:
    get:
      summary: 获取报名批次详情
      description: 项目成员可查看。
      tags: [舞台报名审核]
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
        - name: sid
          in: path
          required: true
          schema: { type: string }
      responses:
        '200':
          description: 报名批次详情
          content:
            application/json:
              schema:
                type: object
                required: [signup]
                properties:
                  signup: { $ref: '#/components/schemas/StageSignup' }
        '401':
          description: 未认证（unauthorized）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '404':
          description: 批次不存在（not_found，含跨项目 sid）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
    patch:
      summary: 更新报名批次
      description: 需 tools:manage 权限。字段均可选，合并后仍需 endAt 晚于 startAt。
      tags: [舞台报名审核]
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
        - name: sid
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name: { type: string }
                startAt: { type: string, format: date-time }
                endAt: { type: string, format: date-time }
                note: { type: string }
      responses:
        '200':
          description: 更新成功
          content:
            application/json:
              schema:
                type: object
                required: [signup]
                properties:
                  signup: { $ref: '#/components/schemas/StageSignup' }
        '400':
          description: 参数错误（bad_request）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '401':
          description: 未认证（unauthorized）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '403':
          description: 无权限（forbidden / api_key_forbidden / api_key_wrong_project）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '404':
          description: 批次不存在（not_found）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
    delete:
      summary: 删除报名批次
      description: 需 tools:manage 权限。
      tags: [舞台报名审核]
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
        - name: sid
          in: path
          required: true
          schema: { type: string }
      responses:
        '200':
          description: 删除成功
          content:
            application/json:
              schema:
                type: object
                required: [ok]
                properties:
                  ok: { type: boolean }
        '401':
          description: 未认证（unauthorized）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '403':
          description: 无权限（forbidden / api_key_forbidden / api_key_wrong_project）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '404':
          description: 批次不存在（not_found）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
  /api/projects/{id}/stage-signups/{sid}/import:
    post:
      summary: 导入已通过节目到 Rundown
      description: 需 tools:manage 权限。目标 Rundown 必须属于本项目；把全部 approved 节目按追加语义（不去重）追加到该 Rundown 条目末尾。无 approved 节目返回 400「没有已通过的节目可导入」。
      tags: [舞台报名审核]
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
        - name: sid
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [rundownId]
              properties:
                rundownId: { type: string, description: 目标 Rundown ID }
      responses:
        '200':
          description: 导入成功，返回更新后的 Rundown（与舞台 Rundown 详情同形，附件已解析）
          content:
            application/json:
              schema:
                type: object
                required: [rundown]
                properties:
                  rundown: { $ref: '#/components/schemas/StageRundown' }
        '400':
          description: 参数错误（bad_request，含「没有已通过的节目可导入」）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '401':
          description: 未认证（unauthorized）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '403':
          description: 无权限（forbidden / api_key_forbidden / api_key_wrong_project）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '404':
          description: 批次或 Rundown 不存在（not_found）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
  /api/projects/{id}/stage-signups/{sid}/items:
    post:
      summary: 新增报名节目
      description: 需 tools:manage 权限。name trim 后非空；durationMin 为 1–1440 整数；participants 每项取 cn/contact trim，过滤空 cn。新节目 status 默认 pending。
      tags: [舞台报名审核]
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
        - name: sid
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [name, durationMin]
              properties:
                name: { type: string, description: 节目名称 }
                durationMin: { type: integer, description: 时长（分钟，1–1440） }
                participants:
                  type: array
                  items:
                    type: object
                    properties:
                      cn: { type: string, description: 参与者称呼 }
                      contact: { type: string, description: 联系方式 }
                note: { type: string, description: 备注 }
      responses:
        '201':
          description: 新增成功
          content:
            application/json:
              schema:
                type: object
                required: [signup]
                properties:
                  signup: { $ref: '#/components/schemas/StageSignup' }
        '400':
          description: 参数错误（bad_request）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '401':
          description: 未认证（unauthorized）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '403':
          description: 无权限（forbidden / api_key_forbidden / api_key_wrong_project）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '404':
          description: 批次不存在（not_found）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
  /api/projects/{id}/stage-signups/{sid}/items/{itemId}:
    patch:
      summary: 更新报名节目
      description: 需 tools:manage 权限。字段均可选，校验同新增。
      tags: [舞台报名审核]
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
        - name: sid
          in: path
          required: true
          schema: { type: string }
        - name: itemId
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name: { type: string }
                durationMin: { type: integer, description: 时长（分钟，1–1440） }
                participants:
                  type: array
                  items:
                    type: object
                    properties:
                      cn: { type: string }
                      contact: { type: string }
                note: { type: string }
      responses:
        '200':
          description: 更新成功
          content:
            application/json:
              schema:
                type: object
                required: [signup]
                properties:
                  signup: { $ref: '#/components/schemas/StageSignup' }
        '400':
          description: 参数错误（bad_request）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '401':
          description: 未认证（unauthorized）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '403':
          description: 无权限（forbidden / api_key_forbidden / api_key_wrong_project）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '404':
          description: 批次或节目不存在（not_found）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
    delete:
      summary: 删除报名节目
      description: 需 tools:manage 权限。
      tags: [舞台报名审核]
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
        - name: sid
          in: path
          required: true
          schema: { type: string }
        - name: itemId
          in: path
          required: true
          schema: { type: string }
      responses:
        '200':
          description: 删除成功，返回更新后的报名批次
          content:
            application/json:
              schema:
                type: object
                required: [signup]
                properties:
                  signup: { $ref: '#/components/schemas/StageSignup' }
        '401':
          description: 未认证（unauthorized）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '403':
          description: 无权限（forbidden / api_key_forbidden / api_key_wrong_project）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '404':
          description: 批次或节目不存在（not_found）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
  /api/projects/{id}/stage-signups/{sid}/items/{itemId}/status:
    patch:
      summary: 拍板节目状态
      description: 需 tools:manage 权限。拍板状态，与投票互不影响；其余状态值 400「无效的状态」。
      tags: [舞台报名审核]
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
        - name: sid
          in: path
          required: true
          schema: { type: string }
        - name: itemId
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [status]
              properties:
                status:
                  type: string
                  enum: [pending, approved, rejected]
      responses:
        '200':
          description: 更新成功
          content:
            application/json:
              schema:
                type: object
                required: [signup]
                properties:
                  signup: { $ref: '#/components/schemas/StageSignup' }
        '400':
          description: 参数错误（bad_request，无效的状态）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '401':
          description: 未认证（unauthorized）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '403':
          description: 无权限（forbidden / api_key_forbidden / api_key_wrong_project）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '404':
          description: 批次或节目不存在（not_found）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
  /api/projects/{id}/stage-signups/{sid}/items/{itemId}/review:
    put:
      summary: 投票（按当前用户 upsert）
      description: 需 tools:manage 权限。已有本人投票则覆盖 decision/comment/updatedAt，否则新增；只记录意见，不改变节目状态；其余 decision 值 400「无效的投票」。
      tags: [舞台报名审核]
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
        - name: sid
          in: path
          required: true
          schema: { type: string }
        - name: itemId
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [decision]
              properties:
                decision:
                  type: string
                  enum: [approve, reject]
                comment: { type: string }
      responses:
        '200':
          description: 投票成功
          content:
            application/json:
              schema:
                type: object
                required: [signup]
                properties:
                  signup: { $ref: '#/components/schemas/StageSignup' }
        '400':
          description: 参数错误（bad_request，无效的投票）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '401':
          description: 未认证（unauthorized）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '403':
          description: 无权限（forbidden / api_key_forbidden / api_key_wrong_project）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '404':
          description: 批次或节目不存在（not_found）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
    delete:
      summary: 撤回本人投票
      description: 需 tools:manage 权限。没有本人投票则幂等返回。
      tags: [舞台报名审核]
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
        - name: sid
          in: path
          required: true
          schema: { type: string }
        - name: itemId
          in: path
          required: true
          schema: { type: string }
      responses:
        '200':
          description: 撤回成功
          content:
            application/json:
              schema:
                type: object
                required: [signup]
                properties:
                  signup: { $ref: '#/components/schemas/StageSignup' }
        '401':
          description: 未认证（unauthorized）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '403':
          description: 无权限（forbidden / api_key_forbidden / api_key_wrong_project）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '404':
          description: 批次或节目不存在（not_found）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
  /api/projects/{id}/lostfound:
    get:
      summary: 列出失物
      description: 项目成员可查看。按 foundAt 倒序，上限 200 条。
      tags: [失物招领]
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
        - name: q
          in: query
          schema: { type: string }
          description: 关键词，大小写不敏感匹配 name/note/foundLocation
        - name: status
          in: query
          schema:
            type: string
            enum: [pending, claimed]
          description: 认领状态过滤，非法值忽略
      responses:
        '200':
          description: 失物列表
          content:
            application/json:
              schema:
                type: object
                required: [items]
                properties:
                  items:
                    type: array
                    items: { $ref: '#/components/schemas/LostFoundItem' }
        '401':
          description: 未认证（unauthorized）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
    post:
      summary: 登记失物
      description: 需 lostfound:manage 权限（project:manage 等价放行，超管不受限）。multipart 表单；name 必填且 trim 后非空；foundAt 可选（非法 400「无效的时间」，缺省为当前时间）；photo 可选单张、仅图片（否则 400「仅支持图片文件」）且 ≤20MB，自动生成 WebP 预览（≤800px、≤100KB）。
      tags: [失物招领]
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required: [name]
              properties:
                name: { type: string, description: 物品名称 }
                note: { type: string, description: 特征描述 }
                foundAt: { type: string, format: date-time, description: 捡到时间，缺省为当前时间 }
                foundLocation: { type: string, description: 捡到地点 }
                photo: { type: string, format: binary, description: 照片，单张，仅图片，≤20MB }
      responses:
        '201':
          description: 登记成功
          content:
            application/json:
              schema:
                type: object
                required: [item]
                properties:
                  item: { $ref: '#/components/schemas/LostFoundItem' }
        '400':
          description: 参数错误（bad_request，名称为空/无效的时间/仅支持图片文件）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '401':
          description: 未认证（unauthorized）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '403':
          description: 无权限（forbidden / api_key_forbidden / api_key_wrong_project）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
  /api/projects/{id}/lostfound/share:
    get:
      summary: 获取失物招领公开分享信息
      description: 需 lostfound:manage 权限。惰性创建分享文档（默认 enabled 为 false + 随机 token，重复 GET token 不变）。公开页地址为 {站点源}/lf/<token>。
      tags: [失物招领]
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
      responses:
        '200':
          description: 分享信息
          content:
            application/json:
              schema:
                type: object
                required: [share]
                properties:
                  share: { $ref: '#/components/schemas/LostFoundShareInfo' }
        '401':
          description: 未认证（unauthorized）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '403':
          description: 无权限（forbidden / api_key_forbidden / api_key_wrong_project）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
    put:
      summary: 更新失物招领公开分享
      description: 需 lostfound:manage 权限。regenerate 为 true 时换新 token，旧链接立即失效。
      tags: [失物招领]
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                enabled: { type: boolean, description: 是否开启公开分享 }
                regenerate: { type: boolean, description: 是否重新生成 token }
      responses:
        '200':
          description: 更新后的分享信息
          content:
            application/json:
              schema:
                type: object
                required: [share]
                properties:
                  share: { $ref: '#/components/schemas/LostFoundShareInfo' }
        '401':
          description: 未认证（unauthorized）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '403':
          description: 无权限（forbidden / api_key_forbidden / api_key_wrong_project）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
  /api/projects/{id}/lostfound/{itemId}:
    get:
      summary: 获取失物详情
      description: 项目成员可查看。
      tags: [失物招领]
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
        - name: itemId
          in: path
          required: true
          schema: { type: string }
      responses:
        '200':
          description: 失物详情
          content:
            application/json:
              schema:
                type: object
                required: [item]
                properties:
                  item: { $ref: '#/components/schemas/LostFoundItem' }
        '401':
          description: 未认证（unauthorized）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '404':
          description: 物品不存在（not_found，跨项目同理）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
    patch:
      summary: 更新失物
      description: 需 lostfound:manage 权限。multipart 表单，字段同登记（均可选）；另支持 removePhoto=1 移除照片（级联删除存储对象与 File 文档）。换新照片时先持久化新照片再删旧照片。
      tags: [失物招领]
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
        - name: itemId
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              properties:
                name: { type: string }
                note: { type: string }
                foundAt: { type: string, format: date-time }
                foundLocation: { type: string }
                photo: { type: string, format: binary, description: 新照片，单张，仅图片，≤20MB }
                removePhoto: { type: string, enum: ['1'], description: 传 1 移除现有照片 }
      responses:
        '200':
          description: 更新成功
          content:
            application/json:
              schema:
                type: object
                required: [item]
                properties:
                  item: { $ref: '#/components/schemas/LostFoundItem' }
        '400':
          description: 参数错误（bad_request，名称为空/无效的时间/仅支持图片文件）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '401':
          description: 未认证（unauthorized）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '403':
          description: 无权限（forbidden / api_key_forbidden / api_key_wrong_project）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '404':
          description: 物品不存在（not_found）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
    delete:
      summary: 删除失物
      description: 需 lostfound:manage 权限。级联删除照片原图/预览图存储对象与 File 文档。
      tags: [失物招领]
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
        - name: itemId
          in: path
          required: true
          schema: { type: string }
      responses:
        '200':
          description: 删除成功
          content:
            application/json:
              schema:
                type: object
                required: [ok]
                properties:
                  ok: { type: boolean }
        '401':
          description: 未认证（unauthorized）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '403':
          description: 无权限（forbidden / api_key_forbidden / api_key_wrong_project）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '404':
          description: 物品不存在（not_found）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
  /api/projects/{id}/lostfound/{itemId}/status:
    patch:
      summary: 流转失物认领状态
      description: 需 lostfound:manage 权限。claimed 记录 claimedAt 并保存 claimNote（未提供则保留原值）；pending 清空 claimedAt 与 claimNote；其余状态值 400「无效的状态」。
      tags: [失物招领]
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
        - name: itemId
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [status]
              properties:
                status:
                  type: string
                  enum: [pending, claimed]
                claimNote: { type: string, description: 认领备注/联系方式，仅项目内可见 }
      responses:
        '200':
          description: 更新成功
          content:
            application/json:
              schema:
                type: object
                required: [item]
                properties:
                  item: { $ref: '#/components/schemas/LostFoundItem' }
        '400':
          description: 参数错误（bad_request，无效的状态）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '401':
          description: 未认证（unauthorized）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '403':
          description: 无权限（forbidden / api_key_forbidden / api_key_wrong_project）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '404':
          description: 物品不存在（not_found）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
  /api/projects/{id}/lostfound/{itemId}/photo:
    get:
      summary: 获取失物照片
      description: 项目成员可读。有 WebP 预览时优先返回预览，否则返回原图；无照片返回 404。
      tags: [失物招领]
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
        - name: itemId
          in: path
          required: true
          schema: { type: string }
      responses:
        '200':
          description: 照片二进制
          content:
            image/*:
              schema: { type: string, format: binary }
        '401':
          description: 未认证（unauthorized）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '404':
          description: 物品或照片不存在（not_found）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
  /api/public/lostfound/{token}:
    get:
      summary: 公开查看失物列表
      description: 免登录，按分享 token 只读暴露，限流 300 次/分钟/IP。分享不存在或未开启返回 404「链接不存在或已关闭」。返回字段为白名单，不含 claimNote/createdBy 等内部字段。
      tags: [失物招领]
      security: []
      parameters:
        - name: token
          in: path
          required: true
          schema: { type: string }
        - name: q
          in: query
          schema: { type: string }
          description: 关键词，大小写不敏感匹配 name/note/foundLocation
        - name: status
          in: query
          schema:
            type: string
            enum: [pending, claimed]
          description: 认领状态过滤，非法值忽略
      responses:
        '200':
          description: 公开失物列表（按 foundAt 倒序，上限 200 条）
          content:
            application/json:
              schema:
                type: object
                required: [projectName, items]
                properties:
                  projectName: { type: string }
                  items:
                    type: array
                    items: { $ref: '#/components/schemas/PublicLostFoundItem' }
        '404':
          description: 链接不存在或已关闭（not_found）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '429':
          description: 触发限流（rate_limited）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
  /api/public/lostfound/{token}/items/{itemId}/photo:
    get:
      summary: 公开获取失物照片
      description: 免登录，限流 300 次/分钟/IP。item 必须属于该分享的项目，跨项目返回 404。
      tags: [失物招领]
      security: []
      parameters:
        - name: token
          in: path
          required: true
          schema: { type: string }
        - name: itemId
          in: path
          required: true
          schema: { type: string }
      responses:
        '200':
          description: 照片二进制（有 WebP 预览时优先预览，否则原图）
          content:
            image/*:
              schema: { type: string, format: binary }
        '404':
          description: 链接不存在或已关闭 / 物品不存在 / 无照片（not_found）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '429':
          description: 触发限流（rate_limited）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
  /api/projects/{id}/custom-tools:
    get:
      summary: 列出自定义工具
      description: 项目成员可查看，按 createdAt 升序；创建者已注销显示「未知用户」。
      tags: [自定义工具]
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
      responses:
        '200':
          description: 自定义工具列表
          content:
            application/json:
              schema:
                type: object
                required: [tools]
                properties:
                  tools:
                    type: array
                    items: { $ref: '#/components/schemas/CustomTool' }
        '401':
          description: 未认证（unauthorized）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
    post:
      summary: 创建自定义工具
      description: 需 tools:manage 权限。name 必填且 ≤50；url 必须可解析且协议为 http/https（≤1000，否则 400 invalid_url）；mode 缺省 embed；description ≤200；scopes 超集静默过滤。
      tags: [自定义工具]
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [name, url]
              properties:
                name: { type: string, description: 工具名称，≤50 字符 }
                url: { type: string, description: 工具地址，仅 http/https，≤1000 字符 }
                description: { type: string, description: 描述，≤200 字符 }
                mode:
                  type: string
                  enum: [embed, link]
                  description: 打开方式，embed 为页内 iframe、link 为新标签页，缺省 embed
                passToken: { type: boolean, description: 打开时是否投递启动令牌，缺省 false }
                scopes:
                  type: array
                  items: { type: string }
                  description: 允许插件使用的权限点，非法项静默丢弃
      responses:
        '201':
          description: 创建成功
          content:
            application/json:
              schema:
                type: object
                required: [tool]
                properties:
                  tool: { $ref: '#/components/schemas/CustomTool' }
        '400':
          description: 参数错误（bad_request / invalid_url）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '401':
          description: 未认证（unauthorized）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '403':
          description: 无权限（forbidden / api_key_forbidden / api_key_wrong_project）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
  /api/projects/{id}/custom-tools/{toolId}:
    patch:
      summary: 更新自定义工具
      description: 需 tools:manage 权限。部分更新，校验同创建。
      tags: [自定义工具]
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
        - name: toolId
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name: { type: string }
                url: { type: string }
                description: { type: string }
                mode:
                  type: string
                  enum: [embed, link]
                passToken: { type: boolean }
                scopes:
                  type: array
                  items: { type: string }
      responses:
        '200':
          description: 更新成功
          content:
            application/json:
              schema:
                type: object
                required: [tool]
                properties:
                  tool: { $ref: '#/components/schemas/CustomTool' }
        '400':
          description: 参数错误（bad_request / invalid_url）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '401':
          description: 未认证（unauthorized）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '403':
          description: 无权限（forbidden / api_key_forbidden / api_key_wrong_project）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '404':
          description: 工具不存在（not_found）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
    delete:
      summary: 删除自定义工具
      description: 需 tools:manage 权限。删除工具并级联作废该工具签发的全部 API 密钥。
      tags: [自定义工具]
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
        - name: toolId
          in: path
          required: true
          schema: { type: string }
      responses:
        '200':
          description: 删除成功
          content:
            application/json:
              schema:
                type: object
                required: [ok]
                properties:
                  ok: { type: boolean }
        '401':
          description: 未认证（unauthorized）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '403':
          description: 无权限（forbidden / api_key_forbidden / api_key_wrong_project）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '404':
          description: 工具不存在（not_found）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
  /api/projects/{id}/custom-tools/{toolId}/launch:
    post:
      summary: 获取工具启动令牌
      description: 项目成员可用。passToken 为 false 时返回 400「该工具未开启身份携带」。令牌为 5 分钟有效 JWT（kind 为 tool-launch），前端经 postMessage 握手投递给组件（embed 走 window.parent、link 走 window.opener），令牌不进 URL。
      tags: [自定义工具]
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
        - name: toolId
          in: path
          required: true
          schema: { type: string }
      responses:
        '200':
          description: 启动令牌
          content:
            application/json:
              schema:
                type: object
                required: [launchToken]
                properties:
                  launchToken: { type: string, description: 5 分钟有效的启动令牌 JWT }
        '400':
          description: 该工具未开启身份携带（bad_request）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '401':
          description: 未认证（unauthorized）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '404':
          description: 工具不存在（not_found）
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      description: 两种 Bearer 凭证：用户 JWT（登录签发，15 分钟，refresh 轮换）与 anonk_ 前缀 API 密钥（OpenAPI 模式）。项目域接口二者皆可；/api/me、/api/admin、/api/push、/api/invites、/api/files、项目创建与全量列表仅接受用户 JWT，API 密钥调用返回 403 api_key_forbidden；密钥跨项目调用返回 403 api_key_wrong_project。
    cronSecret:
      type: http
      scheme: bearer
      description: CRON_SECRET 运维密钥（/api/cron/* 专用）
  schemas:
    Error:
      type: object
      description: 统一错误信封
      required: [error]
      properties:
        error:
          type: object
          required: [code, message]
          properties:
            code: { type: string, description: 机器可读错误码 }
            message: { type: string, description: 人类可读信息 }
    User:
      type: object
      description: 用户
      required: [id, email, name, isSuperAdmin, contacts, onboardedAt]
      properties:
        id: { type: string }
        email: { type: string, format: email }
        name: { type: string }
        isSuperAdmin: { type: boolean }
        contacts:
          type: array
          items:
            type: object
            required: [platform, value]
            properties:
              platform: { type: string }
              value: { type: string }
        onboardedAt: { type: string, format: date-time, nullable: true, description: 新手引导完成时间 }
    ApiKeyInfo:
      type: object
      description: API 密钥元信息（不含原文）
      required: [id, name, projectId, projectName, toolId, toolName, scopes, createdAt, lastUsedAt, expiresAt]
      properties:
        id: { type: string }
        name: { type: string }
        projectId: { type: string }
        projectName: { type: string, description: 项目已删显示「已删除项目」 }
        toolId: { type: string, nullable: true, description: 工具兑换的有值，手动生成为 null }
        toolName: { type: string, nullable: true, description: 工具已删显示「已删除工具」 }
        scopes:
          type: array
          items: { type: string }
        createdAt: { type: string, format: date-time }
        lastUsedAt: { type: string, format: date-time, nullable: true }
        expiresAt: { type: string, format: date-time, nullable: true, description: null = 永久 }
    Role:
      description: 项目角色与权限点集合
      type: object
      required: [name, permissions]
      properties:
        name: { type: string }
        permissions:
          type: array
          items: { type: string }
    Member:
      description: 项目成员
      type: object
      required: [userId, name, email, roleName]
      properties:
        userId: { type: string }
        name: { type: string }
        email: { type: string }
        roleName: { type: string }
    ProjectStatus:
      description: 项目状态
      type: string
      enum: [draft, preparing, active, settling, completed, archived, cancelled]
    HealthStatus:
      description: 项目健康度（计算口径见「风险预警」章节）
      type: string
      enum: [normal, attention, at_risk, critical]
    ProjectSummary:
      description: 项目列表项（含当前用户视角的角色与统计）
      type: object
      required: [id, name, description, status, startDate, endDate, myRole, currentStage, stageProgress, health, todoCompletionRate, activeRiskCount]
      properties:
        id: { type: string }
        name: { type: string }
        description: { type: string }
        status: { $ref: '#/components/schemas/ProjectStatus' }
        startDate: { type: string, format: date-time, nullable: true }
        endDate: { type: string, format: date-time, nullable: true }
        myRole: { type: string, nullable: true }
        currentStage: { type: string, description: 第一个未完成阶段名；全部完成或无阶段时为空字符串 }
        stageProgress:
          type: object
          required: [completed, total]
          properties:
            completed: { type: integer }
            total: { type: integer }
        health: { $ref: '#/components/schemas/HealthStatus' }
        todoCompletionRate: { type: integer, description: 待办完成率（百分比整数） }
        activeRiskCount: { type: integer }
    ProjectDetail:
      description: 项目详情
      type: object
      required: [id, name, description, status, startDate, endDate, location, timezone, currentStage, stages, roles, createdBy]
      properties:
        id: { type: string }
        name: { type: string }
        description: { type: string }
        status: { $ref: '#/components/schemas/ProjectStatus' }
        startDate: { type: string, format: date-time, nullable: true }
        endDate: { type: string, format: date-time, nullable: true }
        location: { type: string }
        timezone: { type: string }
        currentStage: { type: string, description: 第一个未完成阶段名；全部完成或无阶段时为空字符串 }
        stages:
          type: array
          items: { $ref: '#/components/schemas/StageItem' }
        roles:
          type: array
          items: { $ref: '#/components/schemas/Role' }
        createdBy: { type: string }
    StageItem:
      description: 项目阶段
      type: object
      required: [id, name, order, completedAt, note]
      properties:
        id: { type: string }
        name: { type: string }
        order: { type: integer }
        completedAt: { type: string, format: date-time, nullable: true }
        note: { type: string }
    MilestoneItem:
      description: 项目里程碑
      type: object
      required: [id, title, date, description, stageId, stageName, completedAt, createdBy]
      properties:
        id: { type: string }
        title: { type: string }
        date: { type: string, format: date-time }
        description: { type: string }
        stageId: { type: string, nullable: true, description: 关联的项目阶段 id（可空） }
        stageName: { type: string, nullable: true, description: 关联阶段名（可空） }
        completedAt: { type: string, format: date-time, nullable: true }
        createdBy:
          type: object
          required: [userId, name]
          properties:
            userId: { type: string }
            name: { type: string }
    TodoItem:
      description: 待办条目（含完成信息与进度时间线）
      type: object
      required: [id, title, category, assignees, nodeAt, dueAt, remindAt, status, note, createdBy, createdAt, completedAt, completedBy, completionNote, attachments, updates]
      properties:
        id: { type: string }
        title: { type: string }
        category: { type: string }
        assignees:
          type: array
          items:
            type: object
            required: [userId, name]
            properties:
              userId: { type: string }
              name: { type: string }
        nodeAt: { type: string, format: date-time, nullable: true }
        dueAt: { type: string, format: date-time, nullable: true }
        remindAt: { type: string, format: date-time, nullable: true }
        status: { type: string, enum: [open, done] }
        note: { type: string }
        createdBy: { type: string }
        createdAt: { type: string, format: date-time }
        completedAt: { type: string, format: date-time, nullable: true }
        completedBy: { type: string, nullable: true }
        completionNote: { type: string, nullable: true }
        attachments:
          type: array
          items:
            type: object
            required: [id, filename]
            properties:
              id: { type: string }
              filename: { type: string }
        updates:
          type: array
          description: 进度时间线（追加式，不可编辑/删除）
          items:
            type: object
            required: [note, createdBy, createdByName, createdAt, attachments]
            properties:
              note: { type: string }
              createdBy: { type: string }
              createdByName: { type: string }
              createdAt: { type: string, format: date-time }
              attachments:
                type: array
                items:
                  type: object
                  required: [id, filename]
                  properties:
                    id: { type: string }
                    filename: { type: string }
    AnnouncementItem:
      description: 公告条目
      type: object
      required: [id, title, content, type, isPinned, requireConfirmation, publishedBy, publishedAt, expiresAt, confirmedByMe]
      properties:
        id: { type: string }
        title: { type: string }
        content: { type: string }
        type: { type: string, enum: [normal, important, emergency] }
        isPinned: { type: boolean }
        requireConfirmation: { type: boolean }
        publishedBy:
          type: object
          required: [userId, name]
          properties:
            userId: { type: string }
            name: { type: string }
        publishedAt: { type: string, format: date-time }
        expiresAt: { type: string, format: date-time, nullable: true }
        confirmedByMe: { type: boolean, description: 当前用户是否已确认知悉 }
    DashboardSummary:
      description: 仪表盘汇总指标与模块概览（无对应权限点时模块为 null）
      type: object
      required: [metrics, modules]
      properties:
        metrics:
          type: object
          required: [todoCompletionRate, overdueCount, budgetUsageRate, pendingMaterialCount, workConfirmationRate, memberCount, activeRiskCount]
          properties:
            todoCompletionRate: { type: number }
            overdueCount: { type: integer }
            budgetUsageRate: { type: number, nullable: true }
            pendingMaterialCount: { type: integer }
            workConfirmationRate: { type: number }
            memberCount: { type: integer }
            activeRiskCount: { type: integer }
        modules:
          type: object
          required: [todos, finance, materials, work]
          properties:
            todos:
              type: object
              required: [total, done, open, overdue, dueThisWeek, completionRate]
              properties:
                total: { type: integer }
                done: { type: integer }
                open: { type: integer }
                overdue: { type: integer }
                dueThisWeek: { type: integer }
                completionRate: { type: number }
            finance:
              type: object
              nullable: true
              description: 无财务权限点（finance:manage/finance:add）时为 null
              required: [ticketIncomeCents, incomeCents, expenseCents, profitCents]
              properties:
                ticketIncomeCents: { type: integer, description: 金额单位分 }
                incomeCents: { type: integer, description: 金额单位分 }
                expenseCents: { type: integer, description: 金额单位分 }
                profitCents: { type: integer, description: 金额单位分 }
            materials:
              type: object
              nullable: true
              required: [totalResources, noVersionCount, recentCount]
              properties:
                totalResources: { type: integer }
                noVersionCount: { type: integer }
                recentCount: { type: integer }
            work:
              type: object
              nullable: true
              required: [totalModules, totalRequired, totalAssigned, confirmedCount, shortageCount]
              properties:
                totalModules: { type: integer }
                totalRequired: { type: integer }
                totalAssigned: { type: integer }
                confirmedCount: { type: integer }
                shortageCount: { type: integer }
    DashboardActionItem:
      description: 本人待行动项（指派待办或未确认现场任务）
      type: object
      required: [id, sourceType, title, detail, dueAt, isOverdue, action]
      properties:
        id: { type: string }
        sourceType: { type: string, enum: [todo, work] }
        title: { type: string }
        detail: { type: string }
        dueAt: { type: string, format: date-time, nullable: true }
        isOverdue: { type: boolean }
        action: { type: string, enum: [complete, confirm] }
    ScheduleGroup:
      description: 日程分组（按日期聚合）
      type: object
      required: [date, label, items]
      properties:
        date: { type: string, description: YYYY-MM-DD }
        label: { type: string, description: 今天 / 明天 / MM-DD }
        items:
          type: array
          items:
            type: object
            required: [id, sourceType, title, time, allDay]
            properties:
              id: { type: string }
              sourceType: { type: string, enum: [todo, work, project, milestone] }
              title: { type: string }
              time: { type: string }
              allDay: { type: boolean }
    DashboardPreferences:
      description: 工作台个人偏好（无记录时返回默认值）
      type: object
      required: [defaultView, collapsedCards, hiddenCards, scheduleRange, cardOrder]
      properties:
        defaultView: { type: string, enum: [personal, project] }
        collapsedCards:
          type: array
          items: { type: string }
        hiddenCards:
          type: array
          items: { type: string }
        scheduleRange: { type: integer, enum: [7, 30] }
        cardOrder:
          type: array
          items: { type: string }
    ActivityItem:
      description: 项目动态条目
      type: object
      required: [id, actor, type, message, sourceType, sourceId, createdAt]
      properties:
        id: { type: string }
        actor:
          type: object
          required: [userId, name]
          properties:
            userId: { type: string }
            name: { type: string }
        type: { type: string }
        message: { type: string }
        sourceType: { type: string }
        sourceId: { type: string, nullable: true }
        createdAt: { type: string, format: date-time }
    RiskItem:
      description: 风险预警条目（仅 active 与 ignored 状态会被返回）
      type: object
      required: [id, ruleCode, level, sourceType, sourceId, title, description, status, firstDetectedAt, lastDetectedAt, resolvedAt, ignoredBy, ignoredUntil, ignoreReason]
      properties:
        id: { type: string }
        ruleCode: { type: string }
        level: { type: string, enum: [info, warning, critical] }
        sourceType: { type: string, enum: [todo, finance, material, work] }
        sourceId: { type: string, nullable: true }
        title: { type: string }
        description: { type: string }
        status: { type: string, enum: [active, ignored] }
        firstDetectedAt: { type: string, format: date-time }
        lastDetectedAt: { type: string, format: date-time }
        resolvedAt: { type: string, format: date-time, nullable: true }
        ignoredBy: { type: string, nullable: true }
        ignoredUntil: { type: string, format: date-time, nullable: true }
        ignoreReason: { type: string, nullable: true }
    FileMeta:
      description: 文件元信息
      type: object
      required: [id, filename, mime, size]
      properties:
        id: { type: string }
        filename: { type: string }
        mime: { type: string }
        size: { type: integer, description: 字节数 }
    Visibility:
      description: 可见范围；两个数组均为空表示不限制（走权限点判定），非空则仅列出的用户或角色可见且优先于权限点
      type: object
      required: [userIds, roleNames]
      properties:
        userIds:
          type: array
          items: { type: string }
        roleNames:
          type: array
          items: { type: string }
    TxUser:
      description: 账目中的用户引用
      type: object
      required: [userId, name]
      properties:
        userId: { type: string }
        name: { type: string }
    TransactionItem:
      description: 财务账目
      type: object
      required: [id, type, amountCents, note, payer, splitAmong, createdBy, createdByName, createdAt, attachments]
      properties:
        id: { type: string }
        type: { type: string, enum: [income, expense] }
        amountCents: { type: integer, description: 金额（整数分） }
        note: { type: string }
        payer:
          $ref: '#/components/schemas/TxUser'
        splitAmong:
          type: array
          items: { $ref: '#/components/schemas/TxUser' }
          description: 参与平摊人；空数组表示全体成员
        createdBy: { type: string }
        createdByName: { type: string }
        createdAt: { type: string, format: date-time }
        attachments:
          type: array
          items:
            type: object
            required: [id, filename]
            properties:
              id: { type: string }
              filename: { type: string }
    FinanceSummary:
      description: 项目财务汇总（仅财务管理者可见；门票收入视为项目公款，不挂在任何成员名下）
      type: object
      required: [ticketPriceCents, ticketCount, ticketTypes, ticketIncomeCents, incomeCents, expenseCents, profitCents, perUser, settlement]
      properties:
        ticketPriceCents: { type: integer, description: 旧单票字段——票价（分），多票种保存时清零 }
        ticketCount: { type: integer, description: 旧单票字段——售票数量，多票种保存时清零 }
        ticketTypes:
          type: array
          items:
            type: object
            required: [name, priceCents, count]
            properties:
              name: { type: string }
              priceCents: { type: integer }
              count: { type: integer }
        ticketIncomeCents:
          type: integer
          description: 门票总收入（分），等于各票种 priceCents×count 之和加旧单票 ticketPriceCents×ticketCount
        incomeCents: { type: integer, description: 记账收入（不含门票） }
        expenseCents: { type: integer, description: 全部记账支出 }
        profitCents: { type: integer, description: ticketIncomeCents + incomeCents − expenseCents }
        perUser:
          type: array
          description: 覆盖全体项目成员的净额（分）
          items:
            type: object
            required: [userId, name, netCents]
            properties:
              userId: { type: string }
              name: { type: string }
              netCents: { type: integer }
        settlement:
          type: array
          description: 建议转账（净额为负者向为正者转账的贪心结算列表）
          items:
            type: object
            required: [from, to, amountCents]
            properties:
              from: { $ref: '#/components/schemas/TxUser' }
              to: { $ref: '#/components/schemas/TxUser' }
              amountCents: { type: integer }
    ResourceTypeItem:
      description: 物料资源类型
      type: object
      required: [id, name, visibility]
      properties:
        id: { type: string }
        name: { type: string }
        visibility: { $ref: '#/components/schemas/Visibility' }
    ResourceItem:
      description: 物料资源
      type: object
      required: [id, typeId, name, description, visibility, latestVersion, hasPreview, createdAt]
      properties:
        id: { type: string }
        typeId: { type: string }
        name: { type: string }
        description: { type: string }
        visibility: { $ref: '#/components/schemas/Visibility' }
        latestVersion: { type: integer, description: 当前最新版本号，无版本时为 0 }
        hasPreview: { type: boolean, description: 预览接口将返回 200 }
        createdAt: { type: string, format: date-time }
    ResourceVersionItem:
      description: 物料资源版本
      type: object
      required: [version, note, hasPreview, createdBy, createdAt, file]
      properties:
        version: { type: integer }
        note: { type: string }
        hasPreview: { type: boolean, description: 预览接口将返回 200 }
        createdBy: { type: string }
        createdAt: { type: string, format: date-time }
        file:
          description: 版本对应文件；历史遗留版本可能为 null
          type: object
          nullable: true
          required: [id, filename, mime, size]
          properties:
            id: { type: string }
            filename: { type: string }
            mime: { type: string }
            size: { type: integer }
    PhysicalItemStatus:
      description: 物资状态（planned 计划中、in_stock 已入库、in_use 使用中、returned 已归还、disposed 已处置）
      type: string
      enum: [planned, in_stock, in_use, returned, disposed]
    PhysicalCategoryItem:
      description: 实物分类
      type: object
      required: [id, name, order]
      properties:
        id: { type: string }
        name: { type: string }
        order: { type: integer }
    PhysicalItemItem:
      description: 物资条目
      type: object
      required: [id, categoryId, name, spec, unit, plannedQty, onHandQty, usedQty, lostQty, status, responsible, location, tags, note, createdBy, createdAt, updatedAt]
      properties:
        id: { type: string }
        categoryId: { type: string }
        name: { type: string }
        spec: { type: string }
        unit: { type: string }
        plannedQty: { type: integer }
        onHandQty: { type: integer }
        usedQty: { type: integer }
        lostQty: { type: integer }
        status: { $ref: '#/components/schemas/PhysicalItemStatus' }
        responsible:
          type: object
          nullable: true
          required: [userId, name]
          properties:
            userId: { type: string }
            name: { type: string }
        location: { type: string }
        tags:
          type: array
          items: { type: string }
        note: { type: string }
        createdBy: { type: string }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    PhysicalLogItem:
      description: 物资数量/状态变动日志
      type: object
      required: [id, type, qty, status, note, operator, createdAt]
      properties:
        id: { type: string }
        type:
          type: string
          enum: [adjust_on_hand, adjust_used, adjust_lost, status_change]
        qty: { type: integer }
        status:
          nullable: true
          allOf:
            - $ref: '#/components/schemas/PhysicalItemStatus'
        note: { type: string }
        operator:
          type: object
          required: [userId, name]
          properties:
            userId: { type: string }
            name: { type: string }
        createdAt: { type: string, format: date-time }
    PhysicalSummary:
      description: 实物清单汇总（总计 + 按分类聚合）
      type: object
      required: [total, byCategory]
      properties:
        total:
          type: object
          required: [planned, onHand, used, lost, count]
          properties:
            planned: { type: integer }
            onHand: { type: integer }
            used: { type: integer }
            lost: { type: integer }
            count: { type: integer }
        byCategory:
          type: array
          items:
            type: object
            required: [categoryId, planned, onHand, used, lost, count]
            properties:
              categoryId: { type: string }
              planned: { type: integer }
              onHand: { type: integer }
              used: { type: integer }
              lost: { type: integer }
              count: { type: integer }
    WorkAssignee:
      description: 任务模块成员分配与确认状态
      type: object
      required: [userId, name, confirmedAt, confirmedBy, checkedInAt, completedAt]
      properties:
        userId: { type: string }
        name: { type: string }
        confirmedAt: { type: string, format: date-time, nullable: true }
        confirmedBy: { type: string, nullable: true, description: 确认人用户 ID }
        checkedInAt: { type: string, format: date-time, nullable: true, description: 签到时间 }
        completedAt: { type: string, format: date-time, nullable: true, description: 完成时间 }
    WorkModuleItem:
      description: 现场任务模块
      type: object
      required: [id, name, description, location, startAt, endAt, requiredCount, assignees, createdBy, createdAt]
      properties:
        id: { type: string }
        name: { type: string }
        description: { type: string }
        location: { type: string }
        startAt: { type: string, format: date-time, nullable: true }
        endAt: { type: string, format: date-time, nullable: true }
        requiredCount: { type: integer, description: 所需人力 }
        assignees:
          type: array
          items: { $ref: '#/components/schemas/WorkAssignee' }
        createdBy: { type: string }
        createdAt: { type: string, format: date-time }
    WorkSheetData:
      description: 现场任务单（某成员被分配的模块集合）
      type: object
      required: [project, user, generatedAt, items]
      properties:
        project:
          type: object
          required: [id, name]
          properties:
            id: { type: string }
            name: { type: string }
        user:
          type: object
          required: [id, name]
          properties:
            id: { type: string }
            name: { type: string }
        generatedAt: { type: string, format: date-time }
        items:
          type: array
          description: 分配给该成员的模块，按 startAt、createdAt 升序
          items: { $ref: '#/components/schemas/WorkModuleItem' }
    OnsiteIncident:
      description: 现场异常记录
      type: object
      required: [id, category, note, moduleId, moduleName, reporter, status, createdAt]
      properties:
        id: { type: string }
        category:
          type: string
          enum: [equipment, staff, material, venue, safety, other]
        note: { type: string }
        moduleId: { type: string, nullable: true }
        moduleName: { type: string, nullable: true }
        reporter:
          type: object
          required: [userId, name]
          properties:
            userId: { type: string }
            name: { type: string }
        status: { type: string, enum: [open, resolved] }
        createdAt: { type: string, format: date-time }
    StageParticipant:
      description: 舞台节目参与者
      type: object
      required: [cn, contact]
      properties:
        cn: { type: string, description: 圈名（CN） }
        contact: { type: string, description: 联系方式 }
    StageRundownAttachment:
      description: 节目素材附件（复用 File 模型，经 GET /api/files/{id} 下载）
      type: object
      required: [id, filename, mime, size]
      properties:
        id: { type: string }
        filename: { type: string }
        mime: { type: string }
        size: { type: integer, description: 字节数 }
    StageRundownItem:
      description: Rundown 节目条目；顺序即 items 数组下标，起止时间由前端自 startAt 逐项累加 durationMin 推算
      type: object
      required: [id, name, durationMin, participants, attachments, note]
      properties:
        id: { type: string }
        name: { type: string }
        durationMin: { type: integer, description: 时长（分钟） }
        participants:
          type: array
          items: { $ref: '#/components/schemas/StageParticipant' }
        attachments:
          type: array
          items: { $ref: '#/components/schemas/StageRundownAttachment' }
        note: { type: string }
    StageActual:
      description: 节目实际执行记录；每节目至多一条（最近一次执行）
      type: object
      required: [itemId, startedAt, endedAt]
      properties:
        itemId: { type: string }
        startedAt: { type: string, format: date-time }
        endedAt: { type: string, format: date-time, nullable: true }
    StageExecution:
      description: 执行状态（内嵌于 Rundown）；计划数据 startAt/durationMin 执行期不被改写，未开始节目预计时间 = 计划 + shiftMin
      type: object
      required: [status, currentItemId, startedAt, finishedAt, shiftMin, actuals]
      properties:
        status: { type: string, enum: [idle, running, finished] }
        currentItemId: { type: string, nullable: true, description: 当前节目（finished 时为 null） }
        startedAt: { type: string, format: date-time, nullable: true, description: 首个节目实际上场时刻 }
        finishedAt: { type: string, format: date-time, nullable: true }
        shiftMin: { type: integer, description: 顺延累积（分钟，可负表示提前） }
        actuals:
          type: array
          items: { $ref: '#/components/schemas/StageActual' }
    StageRundown:
      description: 舞台 Rundown 完整文档
      type: object
      required: [id, name, startAt, note, items, execution, createdBy, createdAt, updatedAt]
      properties:
        id: { type: string }
        name: { type: string }
        startAt: { type: string, format: date-time, description: 计划开始时刻 }
        note: { type: string }
        items:
          type: array
          items: { $ref: '#/components/schemas/StageRundownItem' }
        execution: { $ref: '#/components/schemas/StageExecution' }
        createdBy: { type: string, description: 创建者用户 id }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    StageRundownSummary:
      description: Rundown 列表摘要
      type: object
      required: [id, name, startAt, note, itemCount, totalDurationMin, executionStatus, createdAt, updatedAt]
      properties:
        id: { type: string }
        name: { type: string }
        startAt: { type: string, format: date-time }
        note: { type: string }
        itemCount: { type: integer }
        totalDurationMin: { type: integer, description: 全部节目时长之和 }
        executionStatus: { type: string, enum: [idle, running, finished] }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    StageScreenShareInfo:
      description: 现场大屏分享信息
      type: object
      required: [enabled, token]
      properties:
        enabled: { type: boolean }
        token: { type: string, description: 公开访问令牌 }
    StageSignupReview:
      description: 报名节目投票记录，userName 由后端按投票者解析（已注销用户显示「未知用户」）
      type: object
      required: [userId, userName, decision, comment, updatedAt]
      properties:
        userId: { type: string }
        userName: { type: string }
        decision:
          type: string
          enum: [approve, reject]
        comment: { type: string }
        updatedAt: { type: string, format: date-time }
    StageSignupItem:
      description: 报名节目
      type: object
      required: [id, name, durationMin, participants, note, status, reviews]
      properties:
        id: { type: string }
        name: { type: string }
        durationMin: { type: integer, description: 时长（分钟） }
        participants:
          type: array
          items: { $ref: '#/components/schemas/StageParticipant' }
        note: { type: string }
        status:
          type: string
          enum: [pending, approved, rejected]
        reviews:
          type: array
          items: { $ref: '#/components/schemas/StageSignupReview' }
    StageSignup:
      description: 报名批次详情
      type: object
      required: [id, name, startAt, endAt, note, items, createdBy, createdAt, updatedAt]
      properties:
        id: { type: string }
        name: { type: string }
        startAt: { type: string, format: date-time }
        endAt: { type: string, format: date-time }
        note: { type: string }
        items:
          type: array
          items: { $ref: '#/components/schemas/StageSignupItem' }
        createdBy: { type: string }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    StageSignupSummary:
      description: 报名批次摘要，availableMin 为 endAt − startAt 折算分钟
      type: object
      required: [id, name, startAt, endAt, note, itemCount, approvedCount, totalDurationMin, availableMin, createdAt, updatedAt]
      properties:
        id: { type: string }
        name: { type: string }
        startAt: { type: string, format: date-time }
        endAt: { type: string, format: date-time }
        note: { type: string }
        itemCount: { type: integer }
        approvedCount: { type: integer }
        totalDurationMin: { type: integer }
        availableMin: { type: integer }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    LostFoundItem:
      description: 失物招领物品（项目内视图）
      type: object
      required: [id, name, note, foundAt, foundLocation, status, claimedAt, claimNote, hasPhoto, createdAt, updatedAt]
      properties:
        id: { type: string }
        name: { type: string }
        note: { type: string, description: 特征描述 }
        foundAt: { type: string, format: date-time, description: 捡到时间 }
        foundLocation: { type: string, description: 捡到地点 }
        status:
          type: string
          enum: [pending, claimed]
          description: 待认领 / 已认领
        claimedAt: { type: string, format: date-time, nullable: true }
        claimNote: { type: string, description: 认领备注/联系方式，仅项目内可见 }
        hasPhoto: { type: boolean }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    PublicLostFoundItem:
      description: 失物招领物品（公开白名单字段，不含 claimNote/createdBy 等内部字段）
      type: object
      required: [id, name, note, foundAt, foundLocation, status, claimedAt, hasPhoto]
      properties:
        id: { type: string }
        name: { type: string }
        note: { type: string }
        foundAt: { type: string, format: date-time }
        foundLocation: { type: string }
        status:
          type: string
          enum: [pending, claimed]
        claimedAt: { type: string, format: date-time, nullable: true }
        hasPhoto: { type: boolean }
    LostFoundShareInfo:
      description: 失物招领公开分享信息
      type: object
      required: [enabled, token]
      properties:
        enabled: { type: boolean }
        token: { type: string }
    CustomTool:
      description: 项目自定义工具
      type: object
      required: [id, name, url, description, mode, passToken, scopes, createdBy, createdAt]
      properties:
        id: { type: string }
        name: { type: string }
        url: { type: string }
        description: { type: string }
        mode:
          type: string
          enum: [embed, link]
          description: embed 为页内 iframe，link 为新标签页
        passToken: { type: boolean, description: 打开时经 postMessage 握手投递 5 分钟启动令牌（不进 URL） }
        scopes:
          type: array
          items: { type: string }
          description: 允许插件使用的权限点（仅 ALL_PERMISSIONS 内项，非法项静默丢弃）
        createdBy:
          type: object
          required: [userId, name]
          properties:
            userId: { type: string }
            name: { type: string, description: 创建者已注销显示「未知用户」 }
        createdAt: { type: string, format: date-time }
